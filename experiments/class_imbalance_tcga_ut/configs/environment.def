Bootstrap: docker
From: python:3.11-slim

%post
    python -m pip install --upgrade pip
    python -m pip install -r /opt/requirements-experiment.txt

%files
    configs/requirements-experiment.txt /opt/requirements-experiment.txt

%runscript
    exec "$@"

