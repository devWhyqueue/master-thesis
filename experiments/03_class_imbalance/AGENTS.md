# 03_class_imbalance

Why class imbalance hurts accuracy, and what causes it. See `../AGENTS.md` for the shared code/configs/tests/report layout.

## 00_damage

| # | Study | Question | Result |
|---|---|---|---|
| 25 | damage_tcga_ut | How fast does BA/NLL/ECE degrade with class prevalence alone? Same G = 20 patients/class and total T = K·640 patches in every arm; exponential profile (exp-02 allocator), ρ ∈ {1, 2, 5, 10, 20, 50, 100}, class rank permuted per draw, plus native shares (N, realized ρ 18.1); logreg, raw + validation TS. TCGA-UT (BRACS: exp-26) | TCGA-UT: slow, accelerating damage. Δ = 0.11 [0.01, 0.21] (ρ 2), 0.57 [0.30, 0.84] (ρ 10), 1.01 [0.67, 1.36] (ρ 20, onset), 2.91 [2.48, 3.34] (ρ 100). Slope 0.18 pp/doubling up to ρ 10, 0.70 beyond. At ρ 100 tail third recall −11.8, head +5.4. N costs 1.18 [0.85, 1.48], on curve (gap −0.25 [−0.57, 0.07]). Raw ECE 5.5 → 17.8 at ρ 100; TS removes 95% of ECE, 81% of NLL increase; temperature 0.84 → 2.35, selected λ 0.1 → ≤1e-3. All 240 fits converged. |
| 26 | damage_bracs | exp-25 prevalence curve on BRACS: same design (exp-25 code), G = 10 patients/class (only 10–13 ADH train patients per split reach depth 160), T = 7·320 = 2,240 patches; ρ ∈ {1, 2, 5, 10, 20, 50, 100}, class rank permuted per draw, plus native shares (N, realized ρ 6.7); logreg, raw + validation TS | BRACS: early, steady, large damage. Δ = 1.15 [0.14, 2.35] (ρ 2, onset), 4.02 [2.50, 5.66] (ρ 10), 7.65 [5.77, 9.78] (ρ 100) from BA(1) = 39.57. Slope 1.15 pp/doubling, no acceleration (1.25 up to ρ 10, 1.11 beyond). At ρ 100 tail recall 38.7 → 7.3, head +13.8. Same tail patches/patient as TCGA-UT at ρ 10 but 7× damage: prior shift under class overlap, not tail depth (untested). N costs 4.05 [2.69, 5.57] ≈ exp-18 5→20 patient gap (4.80), on curve (gap −0.64 [−1.95, 0.68]). Raw NLL 1.56 → 7.01, ECE 12.3 → 35.7 (r1 underconfident, T 0.65); TS removes 96% of NLL, 92% of ECE increase; T → 9.14, ρ 100 picks λ = 1e-6 (grid floor) in 12/30, extended-grid refit confirms. All 240 fits converged. |

## 01_cause

| # | Study | Question | Result |
|---|---|---|---|
| 27 | prior_vs_support_tcga_ut | exp-25's ratio arm r{rho} moves the loss's class prior and the tail's patch support together. Separated per rho into P{rho} (prior only, r1's balanced rows reweighted toward r{rho}'s class shares), S{rho} (support only, r{rho}'s rows reweighted back to balanced), and post-hoc logit-adjusted LP{rho}/Lr{rho}; r1/r{rho} reused from exp-25's stored outputs. TCGA-UT (BRACS: exp-28) | Interaction reading. Damage vs r1 at ρ 100: r 2.91 [2.48, 3.34], P 1.20 [0.87, 1.53], S 0.53 [0.36, 0.70], interaction I = 1.18 [1.00, 1.37] (shares P 0.41, S 0.18, I 0.41); I resolved from ρ 5 on. Up to ρ 20 prior carries most (ρ 20: P 0.73 of r 1.01), S ≈ 0 (intervals ∋ 0). Acceleration: r 0.52 [0.44, 0.60] pp/doubling, S 0.20 [0.16, 0.24], P 0.04 [−0.02, 0.11] (P linear in log ρ); rest from interaction. Tail third at ρ 100: r −11.8, P −5.9, S −3.7. Logit adjustment: Lr recovers ~half of r's damage but stays below S (−0.92 [−1.24, −0.59] at ρ 100); LP lowers P's BA (−0.49 at ρ 100). λ/temperature shift is prior-driven: S keeps λ 0.1, T 0.85 like r1; P goes to λ ≤ 1e-3, T 1.71 at ρ 100. All 360 fits converged. |
| 28 | prior_vs_support_bracs | exp-27 design on BRACS: same P/S/LP/Lr split of exp-26's ratio arm, exp-27 code reused; tests exp-26's untested "prior shift, not tail depth" explanation | Both factors carry the damage, additively in point estimate. Damage vs r1 at ρ 100: r 7.65 [5.77, 9.78], P 4.47 [2.84, 6.30], S 3.29 [1.99, 4.67], I = −0.11 [−1.74, 1.36] (shares P 0.58, S 0.43); I ∋ 0 at all ρ but only ρ 2 within ±1 pp (else unresolved). P share 0.68–0.73 at ρ 5–20; S resolved from ρ 10 (0.87 at 7.7 tail patches/patient, vs ≈0 on TCGA-UT). exp-26 explanation holds only in part: prior larger (3.7× TCGA-UT), but S alone > TCGA-UT's full damage. No acceleration (r −0.14, P −0.29, S 0.38 [−0.04, 0.79]). Rank groups at ρ 100: tail sub-additive (floor, P tail ≈21% from ρ 10), body super-additive, cancel in BA. Lr recovers ~¼ of r's damage, below S by 2.47 [1.30, 3.82]; LP ≈ P (intercept share ∋ 0). λ/T shift prior-driven: S λ 1–10, T ≤ 0.91; P λ ≤ 1e-2 in 24/30, T 5.37 at ρ 100 (r100 9.14). P20/50/100 hit λ 1e-6 in 3/3/4 fits; extended-grid refit of P50/P100 confirms. All 360 fits converged. |
| 29 | tail_assignment_bracs | Latin square over class × rank: per draw, 7 cyclic shifts of exp-26's class order at ρ 10/100 (shift 0 = exp-26 r10/r100, reused; 360 new fits); tests whether damage depends on which class is rare (headroom vs overlap) | Tail class matters, easy classes cost most (headroom, not overlap). D at ρ 100: UDH 5.73 [3.93, 7.52], ADH 5.77 … N 9.37, IC 9.99 [8.26, 11.80] (SD 1.56); ρ 10: 2.56 (ADH) – 5.46 (N). Mean D 4.18/7.87 (≈ exp-26 4.02/7.65). Spearman r1 recall vs D 0.79/0.96; confusability 0.18/−0.29 (n = 7, descriptive). In tail at ρ 100 six classes keep ≤ 4% recall (lose 89–96%), only IC keeps 34%; own loss 82%/60% of D. Overlap shows only in relative loss at ρ 10 (ρ_s −0.68). Own-rank R² 0.73/0.79. All 360 fits converged. |
| 30 | tail_assignment_tcga_ut | exp-29 question on TCGA-UT (30 classes; Latin square infeasible). Part A: per-class piecewise own-recall-vs-own-log-count model fit on exp-25's stored r1/r10/r100/N (random orders), LODO + 10,000 sampled orders. Part B: two fixed r100 orders sorted by exp-25 r1 recall, easy classes in tail (easy_r100) vs hard in tail (hard_r100); 60 new fits | Tail difficulty matters little; separated confusion partners drive it. D_easy 2.41 [1.95, 2.85], D_hard 1.59 [1.22, 1.95], diff 0.82 [0.24, 1.39] (headroom sign, < 1 pp). Both below random-order r100 (2.91): by 0.51 [0.11, 0.93] / 1.33 [0.87, 1.78]; single random draws 1.53–4.69. No tail collapse (THCA keeps 90% at 28 patches); recall follows allocation relative to r1 confusion partner (READ −13.4 at rank 6 below COAD, +6.6 at rank 23 above COAD). Middle third gets same allocations in both orders yet differs 2.03 pp. Per-class model: R² 0.73 in-sample, LODO Spearman 0.04, overpredicts r100 (3.51), out-of-sample wrong order (easy −0.25, hard 6.90). Exploratory partner term: R² 0.84, errors 1.17/0.93, still wrong order. Per-class/partner numbers from post-hoc one-off (report/per_class_orders.json, partner_model.json). All 60 fits converged. |
| 31 | worst_assignment_tcga_ut | Fits the worst-case assignment exp-30's discussion names but leaves unfitted. Directed pair-separation score S = Σ_{c≠d} w_{c→d}·h_c·max(z_{r_d} − z_{r_c}, 0): w row-normalized off-diagonal share of c's r1 test predictions landing on d, h = pooled r1 recall (headroom), z = log-count deviation by rank; argmax (sep) / argmin (tog) by pairwise-swap hill climbing, flip = reversed(sep) as direction control. Pre-registered go/no-go gate (0 new fits) checks S against exp-25/30's three already-observed damages before Part B's 120 fits | Null: the score fails its own gate, so Part B was not submitted and 0 new fits were spent. G1 passes (S_easy 3239 > S_hard 895, matching the observed D 2.41 > 1.59). G2 fails: mean S over exp-25's 30 random orders is 2583, below S_easy 3239, inverting the observed 2.91 > 2.41 > 1.59. G3 (descriptive) Spearman(S_f, D_f) = 0.38 (p = 0.04, n = 30) over those 30 random orders. Reading: sorting every class by r1 recall makes h_c and rank collinear, so the easy order inflates S mechanically without doing more damage — S rewards headroom/rank monotonicity rather than the damage mechanism. Exp-30's "separated confusion partners, headroom underneath" candidate is not supported as a damage predictor in this form; a replacement score needs its own pre-registration (this plan bars a tuning loop). Gate output: `report/precheck.json` |
| 32 | worst_assignment_rate_tcga_ut | Same directed pair-separation score as exp-31, S = Σ_{c≠d} w_{c→d}·h_c·max(z_{r_d} − z_{r_c}, 0), but w is now the absolute confusion rate P(pred d \| true c) (off-diagonal count / c's full row total), not exp-31's row-normalized share; fixes the collinearity between w and h that broke exp-31's gate. Pre-registered gate (0 new fits) plus a D-on-S calibration line fit on exp-25's 30 random r100 draws, held out against exp-30's easy/hard anchors; Part B fits worst (argmax S_rate) / flip (reversed worst) / mild (argmin S_rate) at ρ 100 (90 new fits) | Gate passes and every held-out endpoint resolves. Precheck: G1/G2 pass, Spearman(S,D) 0.58 (LOO 0.52, p < 0.001, n = 30), easy/hard anchors predicted inside their observed CI (2.18 vs 2.41 [1.95, 2.85]; 1.68 vs 1.59 [1.22, 1.95]), rank-vs-headroom in the worst order 0.08 (not just sorting by recall, the failure mode that broke exp-31). Part B (90/90 fits converged): H1 D_worst − D_r100 = 1.60 [1.11, 2.08] pp > 1 pp threshold; H2 D_r100 − D_mild = 1.57 [1.11, 2.03]; H3 D_worst − D_flip = 1.11 [0.47, 1.73] (mechanism: same rank separation, direction matters). D_worst 4.51 [3.93, 5.08] vs. calibration-predicted 4.34 (underestimate by 0.17 pp, within the extrapolation caveat). D_mild vs. D_hard stays unresolved (−0.25 [−0.58, 0.09], CI ∋ 0), as predicted. Mild observed 1.34 vs. predicted 0.97 (damage flattens at the mild end). Report: `report/ci_worst_assignment_rate_tcga_ut.tex` |
| 33 | prior_margin_cross (TCGA-UT + BRACS) | exp-27/28's prior-only damage P(ρ=100) gap between datasets is 3.27 pp (BRACS 4.47 vs TCGA-UT 1.20); untested why. 0 new fits: injects each dataset's own paired r{ρ}'s realized class prior into r1's stored test probabilities, argmax(log p + log π̂), D_sim(ρ) = resulting BA drop; pre-registered gate checks D_sim(100) against the already-fitted P100 interval per dataset. H1 = cross-dataset gap with independent-bootstrap CI; H2 = D_sim(ρ) vs fitted P(ρ) onset; H3 (TCGA-UT only, descriptive) = 500 random + 1 greedy-confused 7-class subsets of TCGA-UT, scored the same way, BRACS's own point overlaid | Gate fails on both datasets: D_sim(100) = 10.55 [9.72, 11.38] vs fitted [0.87, 1.53] on TCGA-UT (≈7×), 14.43 [11.71, 17.42] vs [2.84, 6.30] on BRACS (≈3.2×); label proxy_invalid, no mechanism claim. H1 gap 3.89 [1.12, 7.06] covers the fitted 3.27 pp but is reported only as a coincidence (the two overshoot factors differ). Curves agree only at ρ 2; TS-scaled sensitivity variant lowers but doesn't close the gate. H3: D_sim strongly anti-correlated with subset BA/margin (ρ_s −0.53/−0.55, p < 1e-37); BRACS's own BA (39.6%) and margin (0.27 nats) fall outside the range of every TCGA-UT subset tested, including the most-confused engineered one — a narrower class count alone doesn't explain BRACS's position. Report: `report/ci_prior_margin.tex` |
