\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage[authoryear,round]{natbib}
\usepackage{siunitx}
\sisetup{group-separator={,}, group-minimum-digits=4}

\graphicspath{{outputs/figures/}{../../experiments/class_imbalance/outputs/figures/}}

\title{Extending class imbalance mitigation benchmarks to more datasets}
\author{Yannik Queisler}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
\noindent A prior study benchmarked class-imbalance mitigation methods on TCGA-UT, a pan-cancer whole-slide-image cohort with a native, moderately imbalanced slide distribution \citep{queisler2026imbalanceTCGA}. This report extends the same benchmark machinery to three additional datasets, keeping the frozen Virchow2 representation, a bounded per-slide evidence budget, and the patient-disjoint evaluation protocol fixed, and asks whether the method comparison remains meaningful when the data regime changes. The datasets are chosen to bracket the imbalance spectrum and to vary organ, label type, and annotation form: BRACS, an organ-specific H\&E breast-lesion cohort with a mildly imbalanced native seven-subtype distribution (head-to-tail ${\approx}2.3{:}1$); CAMELYON16, a sentinel lymph-node metastasis cohort whose mask-derived tumor/normal patch distribution is severely imbalanced (${\approx}39{:}1$); and PANDA, a large prostate-biopsy cohort whose native six-class ISUP grade distribution is again only mildly imbalanced (${\approx}2.4{:}1$) but whose task is ordinal grading, evaluated jointly with a provider-consistent tile-level cancer/benign target derived from its masks. Both patch-feature and whole-slide-image feature-bag method families are compared, each within its own prediction regime, and a plain cross-entropy no-mitigation baseline (CE, MIL CE) is added so mitigation gains are measured against an unweighted reference. On BRACS, OKO and CFAL lead the patch regime by a few points of balanced accuracy while every other method---including ProGAN augmentation---is statistically indistinguishable from the unweighted baseline, and in the WSI-bag regime the unweighted MIL CE baseline is the strongest method, with no mitigation method improving on it; both regimes remain substantially miscalibrated after temperature scaling. On CAMELYON16, despite an imbalance an order of magnitude more severe, every method in both regimes reaches near-ceiling discrimination (balanced accuracy \num{0.96}--\num{0.99}) and near-ideal calibration, and no mitigation method separates from the unweighted baseline. BRACS and CAMELYON16 together show that imbalance severity alone predicts neither task difficulty nor the value of mitigation: what governs the headroom for imbalance mitigation under a frozen-feature benchmark is the separability of the classes in the representation, not the head-to-tail ratio. PANDA confirms this within a single mildly imbalanced cohort: holding organ, cohort, and imbalance fixed and varying only the label, the separable tile-level cancer-detection regime is comparatively easy (balanced accuracy ${\approx}\num{0.85}$) while the entangled slide-level six-class ISUP-grading regime stays hard (macro F1 ${\approx}\num{0.58}$), and the unweighted baseline is competitive in both.
\end{abstract}

\setcounter{tocdepth}{2}
\tableofcontents
\clearpage

\section{Introduction}
A companion study compared class-imbalance mitigation methods under the native, moderately imbalanced slide distribution of TCGA-UT (head-to-tail ${\approx}28{:}1$) across 32 cancer types \citep{queisler2026imbalanceTCGA,tomczak2015reviewThe,komura2022universalEncoding}. That benchmark fixes a frozen Virchow2 encoder, a bounded per-slide evidence budget, and patient-disjoint splits, so that the only thing being compared is how each mitigation method reshapes minibatch exposure or the per-example loss. The natural next question is external validity: does the same benchmark machinery stay meaningful when it is pointed at a different dataset, with a different organ, a different label set, and a different native class distribution?

This report takes that step by extending the benchmark to three further datasets on their \emph{native} class distributions, chosen to span the imbalance spectrum and to vary organ, label type, and annotation form. The design is deliberately conservative: the representation family (frozen Virchow2), the bounded per-slide evidence budget, the patient-disjoint split protocol, the method families, and the validation-based model-selection rule are all carried over unchanged, and only the dataset is swapped. A dataset therefore acts as a check on how far the benchmark generalizes rather than as a new experimental variable.

The first dataset is BRACS, an H\&E breast-pathology dataset with \num{547} WSIs and \num{4539} regions of interest (ROIs) from \num{189} patients, annotated by consensus of three board-certified pathologists into three lesion types and seven subtypes \citep{brancati2022bracs}. The atypical categories are clinically important and comparatively scarce, so BRACS is a genuine class-imbalance setting without any resampling, but its native head-to-tail ratio (${\approx}2.3{:}1$) is only mild. The second dataset is CAMELYON16, an organ-specific cohort of sentinel lymph-node sections for breast-cancer metastasis detection \citep{bejnordi2017diagnostic,litjens2018gigascience}. Its pixel-level tumor annotations make it possible to label individual tiles as tumor or normal, and because metastatic tissue is scarce within a section, the resulting native patch distribution is severely imbalanced (${\approx}39{:}1$). The third dataset is PANDA, a large cohort of prostate core-needle biopsies from two institutions, graded on the ordinal six-class ISUP scale \citep{bulten2022panda}. Its native ISUP distribution is again only mildly imbalanced (${\approx}2.4{:}1$), but the task is an ordinal grading problem rather than a subtype or detection problem, and its provider-dependent masks allow a second, tile-level cancer/benign target on the same slides. Together the three datasets test whether the method comparison holds across mild and severe imbalance and across a breast-lesion subtype task, a lymph-node detection task, and a prostate grading task---the organ, label-granularity, and annotation-type variation that stresses a benchmark's generality.

Both regimes from the companion study are carried over. Patch-feature methods include cross-entropy, class weighting, balanced sampling, focal loss, Scholz-style metric losses, CFAL, OKO set learning, and ProGAN synthetic augmentation \citep{scholz2024imbalanceAware}. WSI-bag methods include cross-entropy, class weighting, balanced sampling, focal loss, RankMix, SC-MIL, and MDE-MIL \citep{chen2023rankmix,juyal2024scMil,ling2025mdeMil}. Patch and WSI-bag methods are reported separately because they consume different prediction units and answer regime-specific questions.

\section{Datasets}
\label{sec:datasets}
The benchmark evaluates each dataset on its \emph{native} class distribution: the observed per-class support is measured after a patient-disjoint preparation and used directly, without any resampling. This report covers three datasets chosen to span the imbalance spectrum and to vary organ, label type, and annotation form: BRACS, whose seven breast-lesion subtypes are only mildly imbalanced; CAMELYON16, whose patch-level tumor/normal distribution is severely imbalanced; and PANDA, a large prostate-biopsy cohort whose six-class ISUP grade distribution is again only mildly imbalanced but ordinal and organ-specific. Further datasets can be added under the same protocol.

\subsection{BRACS}
BRACS is an organ-specific H\&E breast-lesion dataset rather than a pan-cancer dataset, and its annotations are lesion-subtype labels rather than cancer-type labels. It contains \num{547} WSIs and \num{4539} ROIs from \num{189} patients, annotated by consensus of three board-certified pathologists \citep{brancati2022bracs}. The labels form three lesion types---benign, atypical, and malignant---and seven subtypes: normal tissue (N), pathological benign (PB), usual ductal hyperplasia (UDH), flat epithelial atypia (FEA), atypical ductal hyperplasia (ADH), ductal carcinoma in situ (DCIS), and invasive carcinoma (IC).

The benchmark uses the seven-subtype target because it is the level at which class imbalance is most relevant. The atypical categories FEA and ADH are clinically important precancerous lesions and are less common than many benign or malignant samples, so collapsing BRACS to the three lesion types would hide part of the rare-class problem. Patient identifiers from the BRACS summary sheet define the split unit, and the prepared benchmark enforces patient-disjoint train, validation, and test partitions across three seeds---matching the leakage-control standard used for TCGA-UT.

BRACS evidence is derived from the ROI set. Each annotated ROI is tiled deterministically into $256\times256$ RGB patches, and each slide's pooled ROI tiles are then capped at the empirical median number of available tiles per slide (\num{360})---the same data-driven per-slide evidence budget used for CAMELYON16 and PANDA---with slides below the median kept in full. Patch methods consume one frozen Virchow2 feature per ROI tile \citep{vorontsov2024virchow,zimmermann2024virchow2}, represented as the concatenation of the CLS token and the mean of the patch tokens ($\num{2560}$-dimensional vectors). The WSI-bag pipeline consumes grouped ROI-tile features as bounded bags, preserving the same patch-versus-bag comparison while avoiding an additional tissue-detection step on full WSIs. This choice prioritizes label fidelity: all training instances originate from pathologist-selected lesion regions rather than from unlabeled background tissue.

After patch extraction, \num{387} of the \num{547} published WSIs contribute at least one annotated ROI tile; the remaining slides have no tiled ROI annotations available. The prepared benchmark covers \num{151} patients, the full \num{4539} ROI metadata records, and \num{99352} extracted patches (Table~\ref{tab:bracs-dataset-summary}). The seven-subtype WSI support spans PB (\num{196} slides) at the head to DCIS (\num{87} slides) at the tail, a native head-to-tail ratio of approximately 2.25:1 (Figure~\ref{fig:bracs-class-distribution}). This is mild imbalance relative to the pan-cancer TCGA-UT cohort, but it reflects the true clinical distribution of breast-lesion subtypes and is used as-is.

\begin{table}[ht]
  \centering
  \begin{minipage}{0.32\linewidth}
    \centering
    \IfFileExists{outputs/figures/bracs_example_patch.jpg}{%
      \includegraphics[width=\linewidth]{bracs_example_patch.jpg}%
    }{%
      \fbox{\parbox{0.85\linewidth}{\centering\vspace{2em}BRACS example ROI patch unavailable.\vspace{2em}}}%
    }
  \end{minipage}
  \hfill
  \begin{minipage}{0.62\linewidth}
    \centering
    \small
    \IfFileExists{outputs/tables/bracs_dataset_summary.tex}{%
      \input{outputs/tables/bracs_dataset_summary.tex}%
    }{%
      \begin{tabular}{ll}
        \toprule
        Property & Value\\
        \midrule
        Patients & \num{151}\\
        WSIs with ROI tiles & \num{387}\\
        Regions of interest & \num{4539}\\
        Subtype labels & N, PB, UDH, FEA, ADH, DCIS, IC\\
        \bottomrule
      \end{tabular}%
    }
  \end{minipage}
  \caption{BRACS dataset properties used by the native benchmark, after patient-disjoint preparation and ROI tiling, alongside an example $256\times256$ ROI tile. Each tile is one frozen Virchow2 feature consumed by the patch regime and a candidate instance in the WSI bag.}
  \label{tab:bracs-dataset-summary}
\end{table}

\begin{figure}[ht]
  \centering
  \IfFileExists{outputs/figures/bracs_class_distribution.png}{%
    \includegraphics[width=0.72\linewidth]{bracs_class_distribution.png}%
  }{%
    \fbox{\parbox{0.70\linewidth}{\centering BRACS native subtype distribution figure unavailable.}}%
  }
  \caption{Native BRACS WSI support by seven-subtype label after patient-disjoint preparation. The distribution is used directly as the benchmark's native class-imbalance setting.}
  \label{fig:bracs-class-distribution}
\end{figure}

\subsection{CAMELYON16}
CAMELYON16 is an organ-specific H\&E dataset of sentinel lymph-node sections for the detection of breast-cancer metastases \citep{bejnordi2017diagnostic,litjens2018gigascience}. It differs from BRACS along the axes that matter for a generality check. Its label is binary---each slide is either \emph{normal} or contains \emph{tumor} (metastasis)---and its expert annotations are supplied as pixel-level masks that mark tumor and normal tissue, rather than as region-of-interest crops. Holding the frozen Virchow2 representation and the patient-disjoint protocol fixed while changing the organ, the label granularity, and, above all, the severity of the native imbalance, CAMELYON16 is the deliberate severe counterpart to BRACS's mild seven-subtype distribution.

The benchmark reuses the \num{398} slides for which pre-extracted $256\times256$ tissue tiles at \num{20}$\times$ magnification are available, out of the \num{399} published slides, comprising \num{160} tumor and \num{238} normal slides. Each slide is its own patient (case), so slide-disjoint train, validation, and test partitions across three seeds coincide with patient-disjoint splits. CAMELYON16 is evaluated in the same two regimes as BRACS, but with regime-specific labels. In the patch regime, each tile is labelled tumor or normal by intersecting it with the published tumor mask; because metastatic regions occupy only a small fraction of the sectioned tissue, tumor tiles are scarce and the native patch distribution is severely imbalanced (head-to-tail ${\approx}39{:}1$; Table~\ref{tab:camelyon16-dataset-summary} and Figure~\ref{fig:camelyon16-class-distribution}). In the WSI-bag regime, each slide is a single bag carrying its slide-level tumor/normal label, which recovers the canonical CAMELYON16 metastasis-detection task.

Two dataset-specific choices follow from this design. First, the \num{20} tumor slides whose annotations are not exhaustive---so that unannotated tumor could be mislabelled as normal---are excluded from the patch regime but retained in the WSI-bag regime, where only the reliable slide-level label is used. Second, the per-slide evidence budget is set data-driven rather than fixed: the WSI-bag size is the empirical median number of available tissue tiles per slide, which for CAMELYON16 is \num{7874} tiles. This is two orders of magnitude larger than the \num{30}-tile budget that the tile-sparse TCGA-UT and BRACS cohorts admit, and reflects that a whole lymph-node section yields far more tissue than a single lesion ROI. Patch evidence is otherwise the same frozen Virchow2 $\num{2560}$-dimensional embedding used throughout the benchmark \citep{vorontsov2024virchow,zimmermann2024virchow2}, computed once per tile and shared by both regimes.

\begin{table}[ht]
  \centering
  \begin{minipage}{0.32\linewidth}
    \centering
    \IfFileExists{outputs/figures/camelyon16_example_patch.jpg}{%
      \includegraphics[width=\linewidth]{camelyon16_example_patch.jpg}%
    }{%
      \fbox{\parbox{0.85\linewidth}{\centering\vspace{2em}CAMELYON16 example tumor tile unavailable.\vspace{2em}}}%
    }
  \end{minipage}
  \hfill
  \begin{minipage}{0.62\linewidth}
    \centering
    \small
    \IfFileExists{outputs/tables/camelyon16_dataset_summary.tex}{%
      \input{outputs/tables/camelyon16_dataset_summary.tex}%
    }{%
      \begin{tabular}{ll}
        \toprule
        Property & Value\\
        \midrule
        WSIs (tumor / normal) & \num{160} / \num{238}\\
        WSI-bag size (median tiles) & \num{7874}\\
        Patch labels & tumor, normal\\
        Patch imbalance ratio & ${\approx}\num{39}{:}1$\\
        \bottomrule
      \end{tabular}%
    }
  \end{minipage}
  \caption{CAMELYON16 dataset properties used by the native benchmark, after patient-disjoint preparation and mask-based patch labelling, alongside an example $256\times256$ tumor tile. The WSI-bag size is the data-driven median of available \num{20}$\times$ tissue tiles per slide.}
  \label{tab:camelyon16-dataset-summary}
\end{table}

\begin{figure}[ht]
  \centering
  \IfFileExists{outputs/figures/camelyon16_class_distribution.png}{%
    \includegraphics[width=0.6\linewidth]{camelyon16_class_distribution.png}%
  }{%
    \fbox{\parbox{0.70\linewidth}{\centering CAMELYON16 native patch distribution figure unavailable.}}%
  }
  \caption{Native CAMELYON16 patch support by tumor/normal label after patient-disjoint preparation and mask-based labelling (log scale). Tumor tiles are roughly \num{39} times scarcer than normal tiles, giving the benchmark its severe native imbalance setting.}
  \label{fig:camelyon16-class-distribution}
\end{figure}

\subsection{PANDA}
PANDA (Prostate cANcer graDe Assessment) is an organ-specific H\&E dataset of prostate core-needle biopsies assembled for automated Gleason grading, and the largest public cohort of its kind, with \num{10616} whole-slide biopsies \citep{bulten2022panda}. It differs from BRACS and CAMELYON16 along every axis a generality check exercises: a third organ (prostate), a third label type (an ordinal six-class grade rather than breast-lesion subtypes or a binary metastasis flag), and a built-in source of heterogeneity, because the slides come from two institutions---Radboud University Medical Center and Karolinska Institute---that differ in staining, scanning, and annotation convention.

The WSI-bag regime uses the canonical PANDA target: the ISUP grade group, an ordinal scale from \num{0} to \num{5} in which grade \num{0} marks benign tissue and grades \num{1}--\num{5} an increasing Gleason-pattern severity. Because most biopsies are benign or low grade, the native ISUP distribution is only mildly imbalanced (head-to-tail ${\approx}2.4{:}1$), placing PANDA near BRACS on the imbalance spectrum but with a coarser, ordinal label. The two providers grade differently---Karolinska biopsies concentrate in the low grades while Radboud biopsies spread more evenly across the scale---so the provider identity is a genuine distribution-shift axis carried inside a single native cohort.

The patch regime uses a provider-consistent binary cancer/benign target derived from the pixel masks. PANDA's expert masks are region annotations whose value semantics are institution-dependent: Radboud masks distinguish stroma, benign epithelium, and Gleason patterns \num{3}--\num{5}, whereas Karolinska masks mark only benign versus cancerous tissue. Collapsing both to a common cancer/benign distinction is the only tile label the two providers share, so each $256\times256$ tile is labelled \emph{cancer} when at least half of its mask cell carries a cancer value and \emph{benign} otherwise. The two regimes therefore answer regime-specific questions with regime-specific labels---slide-level ordinal grading against tile-level cancer detection---mirroring the split already used for CAMELYON16.

Two dataset-specific choices follow from this design. First, PANDA ships only raw pyramidal whole-slide images, with neither pre-extracted tiles nor precomputed features, so tile evidence is generated here: each biopsy is read at the \num{20}$\times$ pyramid level, restricted to tissue tiles by a stain-intensity filter, and cut deterministically into $256\times256$ RGB patches, each embedded once as a frozen Virchow2 feature \citep{vorontsov2024virchow,zimmermann2024virchow2}. Second, to keep frozen-feature extraction tractable on a cohort an order of magnitude larger than the other two, the benchmark draws a stratified subset of approximately \num{2000} biopsies, sampled to preserve the joint distribution of ISUP grade and provider; the per-slide evidence budget is again data-driven, set to the empirical median number of tissue tiles per biopsy (Table~\ref{tab:panda-dataset-summary} and Figure~\ref{fig:panda-class-distribution}). Biopsies whose masks are unavailable keep their slide-level ISUP label for the WSI-bag regime but are excluded from the mask-labelled patch regime, matching the exhaustiveness handling used for CAMELYON16.

\begin{table}[ht]
  \centering
  \begin{minipage}{0.32\linewidth}
    \centering
    \IfFileExists{outputs/figures/panda_example_patch.jpg}{%
      \includegraphics[width=\linewidth]{panda_example_patch.jpg}%
    }{%
      \fbox{\parbox{0.85\linewidth}{\centering\vspace{2em}PANDA example tile unavailable.\vspace{2em}}}%
    }
  \end{minipage}
  \hfill
  \begin{minipage}{0.62\linewidth}
    \centering
    \small
    \IfFileExists{outputs/tables/panda_dataset_summary.tex}{%
      \input{outputs/tables/panda_dataset_summary.tex}%
    }{%
      \begin{tabular}{ll}
        \toprule
        Property & Value\\
        \midrule
        WSIs (biopsies) & \num{2000}\\
        WSI labels & ISUP 0--5\\
        WSI imbalance ratio & ${\approx}\num{2.4}{:}1$\\
        Patch labels & benign, cancer\\
        \bottomrule
      \end{tabular}%
    }
  \end{minipage}
  \caption{PANDA dataset properties used by the native benchmark, after stratified subset selection, patient-disjoint preparation, and mask-based patch labelling, alongside an example $256\times256$ tissue tile. The WSI-bag size is the data-driven median of available \num{20}$\times$ tissue tiles per biopsy.}
  \label{tab:panda-dataset-summary}
\end{table}

\begin{figure}[ht]
  \centering
  \IfFileExists{outputs/figures/panda_class_distribution.png}{%
    \includegraphics[width=0.72\linewidth]{panda_class_distribution.png}%
  }{%
    \fbox{\parbox{0.70\linewidth}{\centering PANDA native ISUP distribution figure unavailable.}}%
  }
  \caption{Native PANDA WSI support by six-class ISUP grade after stratified subset selection and patient-disjoint preparation. The distribution is used directly as the benchmark's native class-imbalance setting.}
  \label{fig:panda-class-distribution}
\end{figure}

\section{Methods}
\label{sec:methods}
The method taxonomy follows the companion benchmark \citep{queisler2026imbalanceTCGA}. Cross-entropy is the no-mitigation reference. Data-level methods change minibatch exposure through balanced sampling or synthetic augmentation. Algorithm-level methods change the per-example loss through class weighting, focal loss, metric-derived objectives, prototype-affinity objectives, or OKO set supervision. WSI-specific methods change how slide bags are represented or mixed.

\begin{table}[ht]
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}p{0.22\linewidth}p{0.24\linewidth}X@{}}
    \toprule
    Family & Methods & Regime\\
    \midrule
    No-mitigation baseline & CE, MIL CE & Patch, WSI bag\\
    Data-level sampling and augmentation & Balanced sampling, RankMix & Patch, WSI bag\\
    Algorithm-level losses & Weighted CE, Focal loss, CFAL & Patch (CFAL); Patch and WSI bag (others)\\
    Hybrid sampling--loss & CE + soft F1, CE + soft MCC, OKO, MDE-MIL & Patch (first three, OKO); WSI bag (MDE-MIL)\\
    Synthetic data generation & ProGAN augmentation & Patch\\
    Representation and architecture & SC-MIL & WSI bag\\
    \bottomrule
  \end{tabularx}
  \caption{Method families evaluated on the native benchmarks (BRACS, CAMELYON16, and PANDA), following the taxonomy of \citet{queisler2026imbalanceTCGA}. CE and MIL CE are unweighted cross-entropy baselines, added so mitigation gains are measured against a genuine no-mitigation reference. Patch and WSI-bag methods are compared only within their own input regime.}
  \label{tab:method-groups}
\end{table}

Full method definitions follow \citet{queisler2026imbalanceTCGA} and \citet{scholz2024imbalanceAware}. RankMix, SC-MIL, and MDE-MIL are WSI-bag methods whose mechanisms operate on bags rather than isolated patch embeddings \citep{chen2023rankmix,juyal2024scMil,ling2025mdeMil}. The plain cross-entropy baselines (CE for the patch regime, MIL CE for the WSI-bag regime) apply uniform class weights and no resampling, so any improvement from a mitigation method is measured relative to doing nothing.

\section{Evaluation Protocol}
\label{sec:evaluation}
The benchmark evaluates each dataset on its native class distribution. Hyperparameters are selected independently for every method: for each candidate configuration the validation macro F1 is averaged over the three patient-disjoint seeds, and the configuration with the highest mean is chosen, with ties broken by mean validation balanced accuracy. A single configuration is therefore fixed per method (shared across seeds). Selected configurations are evaluated on the fixed test split, and results are reported as mean $\pm$ standard deviation across seeds. Macro F1 is the primary discrimination metric because it weights classes equally; balanced accuracy and accuracy are reported as complementary discrimination metrics.

The comparison is intentionally regime-specific. Patch-feature methods are compared only with other patch-feature methods, and WSI-bag methods are compared only with other WSI-bag methods. The two regimes share the patient-disjoint splits, the frozen Virchow2 representation family, and a bounded per-slide evidence budget, but they differ in prediction unit and aggregation mechanism. The evidence budget itself is dataset-specific but follows a common data-driven rule: each dataset caps its per-slide bag at the empirical median number of available tiles per slide---\num{360} for BRACS, \num{7874} for CAMELYON16, and the median tissue-tile count per biopsy for PANDA. These budgets differ by two orders of magnitude because a whole lymph-node section yields far more tissue than a single breast-lesion ROI set or prostate core. The label used within a regime is also dataset-specific where the two prediction units diverge: for PANDA the WSI-bag regime carries the six-class ISUP grade while the patch regime carries the mask-derived binary cancer/benign label, so the same biopsy contributes an ordinal grading target and a tile-level detection target.

Calibration is evaluated alongside discrimination using expected calibration error \citep[ECE;][]{naeini2015obtaining,guo2017calibration}. For each method and seed, a single temperature is fit by minimizing negative log likelihood on the validation split and applied unchanged to the test score matrix before softmax \citep{guo2017calibration}. Tables~\ref{tab:bracs-calibration-patch} and~\ref{tab:bracs-calibration-wsi} report ECE before and after temperature scaling with the fitted temperature.

\section{Results}
\label{sec:results}
Each dataset is evaluated on its own native distribution, with the patch-feature and WSI-bag regimes reported separately. Scores are specific to each dataset's task and are not directly comparable across datasets or to the 32-class TCGA-UT benchmark, because the label set, task difficulty, and cohort size all differ. The three datasets turn out to span the difficulty--imbalance plane---BRACS is mildly imbalanced but hard, CAMELYON16 severely imbalanced but easy, and PANDA holds imbalance fixed while spanning both difficulty extremes across its two regimes---and it is this spread, rather than any dataset alone, that tests the benchmark's generality.

\subsection{BRACS}
The patch-feature and WSI-bag regimes are reported separately (Tables~\ref{tab:bracs-patch-results} and~\ref{tab:bracs-wsi-results}). The absolute discrimination and calibration numbers are markedly lower than on TCGA-UT for two concrete reasons: fine-grained breast-lesion subtyping (distinguishing e.g.\ FEA, ADH, and DCIS) is intrinsically harder than pan-cancer type classification, and the native breast-only training pool is far smaller than the large constructed pan-cancer pools used for TCGA-UT, so each method has both a harder decision boundary and less data to fit it. This is consistent with the external literature: even recent task-specific BRACS models---purpose-built graph and transformer architectures trained end-to-end for this task---report only mid-\num{60}\% weighted F1 on seven-subtype ROI classification (HACT-Net \num{61.5}\%, ScoreNet \num{64.4}\%), with the atypical UDH and ADH subtypes still around \num{45}\% F1 \citep{pati2022hierarchical,stegmuller2023scorenet}. A frozen-feature benchmark under a bounded evidence budget should therefore not be expected to approach TCGA-UT-level discrimination; the binding limitation is fine-grained lesion morphology and limited task-specific representation learning, not class imbalance alone.

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_result_summary_patch.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/bracs_result_summary_patch.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{BRACS patch result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native patch-feature test results (accuracy, balanced accuracy, and macro F1) for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:bracs-patch-results}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_result_summary_wsi_bag.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/bracs_result_summary_wsi_bag.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{BRACS WSI-bag result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native WSI-bag test results for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:bracs-wsi-results}
\end{table}

In the patch regime, OKO and CFAL are the two strongest methods and are effectively tied at the top: OKO reaches the highest balanced accuracy (\num{0.502}) while CFAL reaches the highest overall accuracy (\num{0.589}) and macro F1 (\num{0.490}) at a near-identical balanced accuracy (\num{0.497}). Both lead the unweighted CE baseline (BAcc \num{0.451}, F1 \num{0.451}) by roughly \num{5} points of balanced accuracy. The remaining methods---CE + soft F1 (BAcc \num{0.480}), CE + soft MCC (\num{0.472}), balanced sampling (\num{0.470}), Weighted CE (\num{0.461}), focal loss (\num{0.460}), and ProGAN augmentation (\num{0.460})---fall between the baseline and the two leaders and are individually indistinguishable from CE within one standard deviation ($\sigma \approx 0.04$--$0.06$). ProGAN augmentation in particular lands squarely on the CE baseline (BAcc \num{0.460}, F1 \num{0.457}), neither helping nor hurting: BRACS's mild native imbalance never reaches the tail-scarcity regime under which synthetic minority augmentation paid off on the companion TCGA-UT benchmark. The overall picture is that only OKO and CFAL clearly separate from unweighted cross-entropy, while every other mitigation method---including ProGAN---provides no measurable lift on BRACS's mild native imbalance.

In the WSI-bag regime, the unweighted MIL CE baseline is the strongest method overall, taking both the highest balanced accuracy (\num{0.524}) and the highest macro F1 (\num{0.508}); Weighted CE follows closely (BAcc \num{0.510}, F1 \num{0.501}), and focal loss attains the highest overall accuracy (\num{0.691}) at a lower balanced accuracy (\num{0.490}). No mitigation method exceeds the no-mitigation baseline on the balanced metrics, and several fall well below it: MDE-MIL (BAcc \num{0.489}), balanced sampling (\num{0.449}), RankMix (\num{0.437}), and SC-MIL, which is the weakest method in the regime by a wide margin (BAcc \num{0.367}, F1 \num{0.383}). Between-seed standard deviations are large (up to $\sigma \approx 0.09$--$0.10$ on balanced accuracy for MIL CE, Weighted CE, and MDE-MIL), so the close ordering at the top should be read as a numerical lead rather than a statistically separated outcome; the qualitative result---that plain MIL CE is at least as good as every dedicated bag-composition and resampling method on BRACS---is exactly the failure mode a no-mitigation baseline is included to expose.

\paragraph{Probability Calibration.}
\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_calibration_patch.tex}{%
    \input{outputs/tables/bracs_calibration_patch.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{BRACS patch calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native patch-feature calibration: ECE before (raw) and after (TS) temperature scaling, with the fitted temperature $T$, mean $\pm$ standard deviation across seeds.}
  \label{tab:bracs-calibration-patch}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_calibration_wsi_bag.tex}{%
    \input{outputs/tables/bracs_calibration_wsi_bag.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{BRACS WSI-bag calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native WSI-bag calibration: ECE before and after temperature scaling, with the fitted temperature $T$. Columns as in Table~\ref{tab:bracs-calibration-patch}.}
  \label{tab:bracs-calibration-wsi}
\end{table}

In the patch regime, raw (pre-scaling) miscalibration is severe for most methods: ECE ranges from about \num{0.29} to \num{0.42}, requiring large temperature corrections ($T \approx 3$--\num{9}) to reach post-scaling ECE of \num{0.02}--\num{0.07}. OKO is the exception, with by far the lowest raw ECE (\num{0.045}) and a fitted temperature close to one ($T\approx\num{0.88}$), consistent with its behaviour on the companion TCGA-UT benchmark. CFAL again shows the CFAL-specific pattern of a near-degenerate low temperature ($T\approx\num{0.08}$) alongside a high raw ECE (\num{0.417}), echoing its affinity-based outputs being nearly flat prior to scaling, which scaling nonetheless corrects to \num{0.049}. After scaling, balanced sampling is the best-calibrated method (ECE \num{0.021}), followed closely by CE + soft MCC (\num{0.029}), focal loss (\num{0.030}), and the unweighted CE baseline (\num{0.031}); CE + soft F1 is the worst (\num{0.070}, $T\approx\num{8.5}$). ProGAN augmentation calibrates like the unweighted baselines (raw ECE \num{0.358}, $T\approx\num{6.2}$, post-scaling ECE \num{0.039}), showing no distinctive miscalibration pattern. In the WSI-bag regime, calibration is uniformly worse than in the patch regime and than on the companion TCGA-UT benchmark: post-scaling ECE ranges from \num{0.136} (focal loss, the best-calibrated method) to \num{0.168} (balanced sampling and SC-MIL, tied for worst), with the unweighted MIL CE baseline at \num{0.165}. RankMix has the lowest raw ECE (\num{0.151}) and smallest temperature ($T\approx\num{1.3}$) of any WSI-bag method, but its post-scaling ECE (\num{0.144}) is not the best. The elevated residual miscalibration across all BRACS methods, relative to TCGA-UT, reflects the smaller training pool and the intrinsic difficulty of seven-class fine-grained subtyping rather than a failure specific to any one method.

\subsection{CAMELYON16}
On CAMELYON16 the picture inverts: the same frozen features that struggle with BRACS's fine-grained subtypes separate tumor from normal tissue almost perfectly, so despite an imbalance an order of magnitude more severe, every method reaches near-ceiling discrimination in both regimes (Tables~\ref{tab:camelyon16-patch-results} and~\ref{tab:camelyon16-wsi-results}).

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/camelyon16_result_summary_patch.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/camelyon16_result_summary_patch.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{CAMELYON16 patch result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{CAMELYON16 native patch-feature test results (tumor vs.\ normal tiles) for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:camelyon16-patch-results}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/camelyon16_result_summary_wsi_bag.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/camelyon16_result_summary_wsi_bag.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{CAMELYON16 WSI-bag result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{CAMELYON16 native WSI-bag test results (slide-level metastasis detection) for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:camelyon16-wsi-results}
\end{table}

In the patch regime, balanced accuracy ranges from \num{0.961} (CFAL) to \num{0.986} (CE + soft F1) and macro F1 from \num{0.940} to \num{0.966}, on \num{99.5}--\num{99.7}\% accuracy. The unweighted CE baseline (BAcc \num{0.962}, F1 \num{0.958}) sits squarely inside this band, and no mitigation method separates from it by more than the between-seed noise: CFAL edges ahead on macro F1 (\num{0.966}) and CE + soft F1 on balanced accuracy (\num{0.986}), but the spread across the whole field is under \num{0.03} on either metric. The severe native imbalance (${\approx}39{:}1$) does not translate into a hard problem, because tumor and normal tiles are highly separable in the frozen Virchow2 space; there is little error left for any reweighting or resampling scheme to remove.

The WSI-bag regime tells the same story at the slide level. All seven methods score between balanced accuracy \num{0.965} and \num{0.984} and macro F1 \num{0.965} and \num{0.982}. MDE-MIL and SC-MIL lead (both F1 \num{0.982}), the unweighted MIL CE baseline (BAcc \num{0.965}, F1 \num{0.965}, tied with Weighted CE) is again fully competitive, and the entire field is separated by less than \num{0.02} macro F1. Canonical slide-level metastasis detection is nearly solved from a bounded bag of Virchow2 features, leaving no meaningful gap for bag-composition or resampling methods to close.

\paragraph{Probability Calibration.}
\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/camelyon16_calibration_patch.tex}{%
    \input{outputs/tables/camelyon16_calibration_patch.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{CAMELYON16 patch calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{CAMELYON16 native patch-feature calibration: ECE before (raw) and after (TS) temperature scaling, with the fitted temperature $T$, mean $\pm$ standard deviation across seeds.}
  \label{tab:camelyon16-calibration-patch}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/camelyon16_calibration_wsi_bag.tex}{%
    \input{outputs/tables/camelyon16_calibration_wsi_bag.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{CAMELYON16 WSI-bag calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{CAMELYON16 native WSI-bag calibration: ECE before and after temperature scaling, with the fitted temperature $T$. Columns as in Table~\ref{tab:camelyon16-calibration-patch}.}
  \label{tab:camelyon16-calibration-wsi}
\end{table}

\noindent Calibration follows discrimination: because the models are both confident and correct, they are well calibrated out of the box. In the patch regime raw ECE is \num{0.003}--\num{0.005} for every method except CFAL, and temperature scaling brings it to \num{0.001}--\num{0.002} (Table~\ref{tab:camelyon16-calibration-patch}); the fitted temperatures ($T\approx\num{3}$--\num{5}) merely soften mildly over-confident logits. CFAL is again the lone exception, with the same near-degenerate low-temperature signature seen on BRACS (raw ECE \num{0.464}, $T\approx\num{0.05}$, post-scaling ECE \num{0.068}). In the WSI-bag regime raw ECE is \num{0.013}--\num{0.027} and post-scaling ECE \num{0.017}--\num{0.034} (Table~\ref{tab:camelyon16-calibration-wsi}); MDE-MIL is the best calibrated (raw ECE \num{0.013}), and for several methods the fitted temperature reaches the grid's lower floor ($T\approx\num{0.05}$) without lowering ECE, reflecting how little miscalibration remains to correct on the small \num{398}-slide cohort. Both regimes are markedly better calibrated than BRACS---patch post-scaling ECE around \num{0.001} versus BRACS's \num{0.04}--\num{0.08}, and WSI-bag around \num{0.02}--\num{0.03} versus BRACS's \num{0.11}--\num{0.15}---which is the calibration counterpart of the same separability gap.

\subsection{PANDA}
PANDA is reported in the same two regimes on its regime-specific labels: slide-level six-class ISUP grading in the WSI-bag regime (Table~\ref{tab:panda-wsi-results}) and tile-level binary cancer detection in the patch regime (Table~\ref{tab:panda-patch-results}), with calibration in Tables~\ref{tab:panda-calibration-patch} and~\ref{tab:panda-calibration-wsi}. The two regimes are placed deliberately on opposite sides of the difficulty axis \emph{within one mildly imbalanced cohort}: binary cancer/benign detection is the kind of coarse tissue distinction that strong pathology features usually separate cleanly, whereas six-class ISUP grading from a bounded bag of frozen features is intrinsically hard---automated Gleason grading is a fine-grained ordinal problem on which even purpose-built, end-to-end models trained for the PANDA challenge reach only moderate agreement with expert consensus \citep{bulten2022panda}. PANDA therefore serves as a within-dataset test of the report's central pattern: if class separability in the frozen representation, rather than the head-to-tail ratio, is what governs the headroom for imbalance mitigation, then a single cohort at a fixed mild imbalance should still show a large gap between an easy, separable detection regime and a hard, entangled grading regime.

In the patch regime, the mask-derived cancer/benign detection task is comparatively separable: every method reaches balanced accuracy between \num{0.832} and \num{0.860} and macro F1 between \num{0.811} and \num{0.866} (Table~\ref{tab:panda-patch-results}). The unweighted CE baseline sits squarely inside this band---second on macro F1 (\num{0.859}) behind only CFAL (\num{0.866}) and mid-field on balanced accuracy (\num{0.838})---so no mitigation method separates from doing nothing by more than the between-seed noise. The methods that explicitly target the minority class buy a few points of balanced accuracy at a matching macro-F1 and accuracy cost: OKO (BAcc \num{0.860}), focal loss (\num{0.854}), and the soft-F1 and soft-MCC objectives (both \num{0.852}) edge above CE on balanced accuracy, while CFAL and CE lead on macro F1 and overall accuracy. ProGAN augmentation, the one synthetic-data method, lands essentially on the CE baseline (balanced accuracy \num{0.839}, macro F1 \num{0.855}), neither helping nor hurting. The result is a tight cluster with no dominant method---the signature of a separable problem at mild imbalance.

The WSI-bag regime inverts the difficulty. Six-class ISUP grading from a bounded bag of \num{421} Virchow2 features is hard: macro F1 spans only \num{0.559}--\num{0.590} and balanced accuracy \num{0.565}--\num{0.588} (Table~\ref{tab:panda-wsi-results}), roughly \num{0.27} below the detection regime on the very same biopsies. The unweighted MIL CE baseline (BAcc \num{0.566}, F1 \num{0.565}) again sits within noise of the field; balanced sampling (F1 \num{0.590}), focal loss (\num{0.583}), and MDE-MIL (\num{0.581}) lead it by about \num{0.02}, but between-seed standard deviations of \num{0.02}--\num{0.04} make these numerical leads rather than statistically separated outcomes, and the validation-selected weighted-CE configuration collapses exactly onto the unweighted baseline. As elsewhere in the benchmark, the method choice matters only at the margin, and no mitigation method clearly beats the no-mitigation reference.

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/panda_result_summary_patch.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/panda_result_summary_patch.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{PANDA patch result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{PANDA native patch-feature test results (cancer vs.\ benign tiles) for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:panda-patch-results}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/panda_result_summary_wsi_bag.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/panda_result_summary_wsi_bag.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{PANDA WSI-bag result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{PANDA native WSI-bag test results (slide-level six-class ISUP grading) for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:panda-wsi-results}
\end{table}

\paragraph{Probability Calibration.}
Calibration follows separability, as on the other datasets. In the patch regime raw ECE is low for most methods (\num{0.024}--\num{0.103}) and temperature scaling brings every method to \num{0.005}--\num{0.036} (Table~\ref{tab:panda-calibration-patch}); OKO is the best calibrated (post-scaling ECE \num{0.005}), and CFAL again shows the near-degenerate low-temperature signature seen on BRACS and CAMELYON16 (raw ECE \num{0.382}, $T\approx\num{0.06}$) that scaling nonetheless corrects to \num{0.011}. In the harder WSI-bag regime raw miscalibration is substantially larger (ECE \num{0.129}--\num{0.252}) and residual post-scaling ECE higher (\num{0.039}--\num{0.097}; Table~\ref{tab:panda-calibration-wsi}), with balanced sampling the best calibrated (\num{0.039}) and MDE-MIL the worst (\num{0.097})---the calibration counterpart of the same detection-versus-grading separability gap.
\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/panda_calibration_patch.tex}{%
    \input{outputs/tables/panda_calibration_patch.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{PANDA patch calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{PANDA native patch-feature calibration: ECE before (raw) and after (TS) temperature scaling, with the fitted temperature $T$, mean $\pm$ standard deviation across seeds.}
  \label{tab:panda-calibration-patch}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/panda_calibration_wsi_bag.tex}{%
    \input{outputs/tables/panda_calibration_wsi_bag.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{PANDA WSI-bag calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{PANDA native WSI-bag calibration: ECE before and after temperature scaling, with the fitted temperature $T$. Columns as in Table~\ref{tab:panda-calibration-patch}.}
  \label{tab:panda-calibration-wsi}
\end{table}

\section{Discussion}
\label{sec:discussion}
The benchmark's central design change from the companion TCGA-UT study is the addition of an unweighted no-mitigation baseline (CE, MIL CE), and the two regimes answer the question it was added to ask quite differently. In the patch regime, most mitigation methods do not clearly separate from the CE baseline on BRACS's mild native imbalance (${\approx}2.3{:}1$)---Weighted CE, focal loss, CE + soft MCC, balanced sampling, and ProGAN augmentation all sit within noise of unweighted cross-entropy. Only OKO and CFAL produce a measurable gain, and even they lead by only a few points of balanced accuracy. This is a plausible consequence of the imbalance itself being mild: with a native ratio an order of magnitude below TCGA-UT's ${\approx}28{:}1$, there may simply be less headroom for reweighting or resampling to help. ProGAN is again informative: on TCGA-UT it led at the mildest constructed severity before collapsing as tail-class support fell to single-digit patch counts at the harshest severity. BRACS's native imbalance never reaches that tail-scarcity regime, so synthetic minority augmentation has no deficit to correct, and ProGAN lands squarely on the CE baseline (BAcc \num{0.460} versus \num{0.451}), neither helping nor hurting. PANDA's cancer/benign patch regime reproduces this at a second mild native imbalance (${\approx}\num{4.7}{:}1$): ProGAN augmentation lands within between-seed noise of the CE baseline (macro F1 \num{0.855} versus \num{0.859}, balanced accuracy \num{0.839} versus \num{0.838}), once more falling short of the tail-scarcity regime where synthetic minority augmentation paid off on TCGA-UT.

The WSI-bag regime tells a sharper version of the same story: the unweighted MIL CE baseline is the single best method on the balanced metrics, and every dedicated mitigation method---balanced sampling, RankMix, MDE-MIL, and most strikingly SC-MIL---scores at or \emph{below} it. This is exactly the failure mode a no-mitigation baseline is meant to surface, and it would have been invisible in a benchmark that only compared mitigation methods against each other---the companion TCGA-UT report, which lacked this baseline, could not have detected it. Whether this reflects BRACS's smaller training pool (283 native training WSIs from 387 prepared slides, versus TCGA-UT's constructed pools of over 1000 slides), the milder native imbalance giving resampling less to correct, or a genuine difference in how bag-composition methods behave outside a pan-cancer setting cannot be distinguished from a single additional dataset; it is a hypothesis for the next dataset in this benchmark to test.

Calibration adds a second, independent axis on which BRACS is harder than TCGA-UT: even the best-calibrated method in each regime (balanced sampling for patch, focal loss for WSI-bag) needs temperature scaling to reach an ECE that is still \num{2}--\num{3}$\times$ worse than typical TCGA-UT post-scaling values, and the gap is especially large in the WSI-bag regime. This is consistent with the smaller training pool and finer-grained label set rather than a method-specific weakness, since the pattern holds across every method in both regimes.

CAMELYON16 sharpens the central lesson by inverting BRACS's regime. It is severely imbalanced (${\approx}39{:}1$, an order of magnitude beyond BRACS and comparable to TCGA-UT's ${\approx}28{:}1$) yet nearly solved: every method in both regimes exceeds \num{0.96} balanced accuracy, the unweighted baselines are competitive with the best mitigation method, and calibration is near-ideal without any dataset-specific tuning. Read together, BRACS and CAMELYON16 show that imbalance severity alone determines neither task difficulty nor the headroom available to mitigation.

PANDA is included to test that separability reading with a stronger control than a cross-dataset contrast can provide. Because BRACS and CAMELYON16 differ simultaneously in imbalance ratio, organ, cohort size, and label type, a difference in mitigation headroom between them cannot be attributed to separability alone with certainty. PANDA holds all of those factors fixed and varies only the label: the same biopsies, at the same mild native imbalance and under the same protocol, carry a coarse tile-level cancer/benign detection target and a fine-grained slide-level ISUP grading target. If separability rather than the head-to-tail ratio governs the headroom for mitigation, the easy detection regime and the hard grading regime should behave differently despite sharing a cohort and an imbalance ratio. That is what the results show: the separable cancer/benign detection regime reaches balanced accuracy around \num{0.85}, whereas the entangled six-class grading regime reaches macro F1 only around \num{0.58} (Tables~\ref{tab:panda-patch-results} and~\ref{tab:panda-wsi-results}), a gap of roughly \num{0.27} produced by label granularity alone at a fixed, mild imbalance, and in both regimes the unweighted baseline stays within between-seed noise of the best mitigation method. PANDA thus reproduces the separability pattern inside a single cohort, ruling out imbalance ratio, organ, and cohort size as the driver. BRACS is mildly imbalanced but hard, because fine-grained lesion subtypes are poorly separated by the frozen features; CAMELYON16 is severely imbalanced but easy, because tumor and normal tissue are nearly separable in those same features. What governs whether a mitigation method can help is therefore the separability of the classes in the representation, not the head-to-tail ratio: a severely imbalanced but separable problem leaves a plain cross-entropy baseline essentially optimal, whereas a mildly imbalanced but entangled problem is exactly where the method choice, and the no-mitigation reference, come to matter. This is a direct caution against selecting or ranking imbalance-mitigation methods by imbalance ratio alone, and it is only visible because the benchmark now spans both corners of the difficulty--imbalance plane.

\section{Conclusion}
\label{sec:conclusion}
This report extended the class-imbalance mitigation benchmark introduced for TCGA-UT \citep{queisler2026imbalanceTCGA} to three additional datasets on their native distributions, keeping the frozen Virchow2 representation, a bounded per-slide evidence budget, and the patient-disjoint evaluation protocol unchanged, and adding an unweighted cross-entropy baseline (CE, MIL CE) so mitigation gains are measured against doing nothing. The datasets were chosen to bracket the imbalance spectrum and to vary organ, label type, and annotation form. On BRACS's native seven-subtype distribution (${\approx}2.3{:}1$ head-to-tail), the patch regime shows OKO and CFAL leading by a few points of balanced accuracy (BAcc \num{0.502} and \num{0.497} vs.\ \num{0.451} for CE), with every other mitigation method---including ProGAN augmentation---statistically indistinguishable from the no-mitigation baseline; in the WSI-bag regime the unweighted MIL CE baseline is the strongest method overall, and no mitigation method improves on it. On CAMELYON16's severely imbalanced patch distribution (${\approx}39{:}1$), by contrast, every patch and WSI-bag method reaches near-ceiling discrimination (balanced accuracy \num{0.96}--\num{0.99}) and near-ideal calibration, and the unweighted baselines are competitive with the best mitigation method in both regimes. PANDA adds a within-cohort control that holds imbalance, organ, cohort, and protocol fixed while varying only the label: on the same prostate biopsies the separable tile-level cancer/benign detection regime reaches balanced accuracy ${\approx}\num{0.85}$ while the entangled slide-level six-class ISUP-grading regime reaches macro F1 only ${\approx}\num{0.58}$ (Tables~\ref{tab:panda-patch-results} and~\ref{tab:panda-wsi-results}), a separability-driven gap at fixed mild imbalance with the no-mitigation baseline competitive in both regimes.

Taken together, the three datasets show that method rankings are not universal and, more pointedly, that imbalance severity alone does not predict whether mitigation helps: CAMELYON16 is far more imbalanced than BRACS yet leaves essentially no headroom for mitigation, because its classes are separable in the frozen representation, whereas BRACS's milder imbalance coincides with a genuinely hard, entangled task where the method choice and the no-mitigation reference matter; and PANDA reproduces the same separability effect inside a single cohort, where an easy detection regime and a hard grading regime diverge sharply at one fixed mild imbalance. What governs the value of imbalance mitigation under a frozen-feature benchmark is therefore class separability, not the head-to-tail ratio---a finding that only becomes visible with a native additional dataset, a genuine no-mitigation baseline, and coverage of both corners of the difficulty--imbalance plane. Further datasets can be added under the same protocol to test how far this pattern generalizes.

\clearpage
\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
