\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage[authoryear,round]{natbib}
\usepackage{siunitx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}
\sisetup{group-separator={,}, group-minimum-digits=4}

\graphicspath{{outputs/figures/}{../../experiments/class_imbalance/outputs/figures/}}

\title{Controlled Class-Imbalance Construction for TCGA-UT}
\author{Yannik Queisler}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
\noindent Class imbalance in computational pathology is not only a question of how many examples a class contributes. A class may perform poorly because it is rare, because its frozen foundation-model features overlap with other classes, or because both effects coincide. The companion TCGA-UT imbalance benchmark established the dataset, the mitigation taxonomy, and patch-level and WSI-bag evaluations under the native TCGA-UT long tail. This report defines a controlled counterpart: artificial full-scale TCGA-UT training distributions that keep all 32 cancer types, preserve fixed validation and test splits, and vary only the training support assigned to each class. Power-law training distributions are generated under native-prevalence, easy-to-difficult, and difficult-to-easy class orders, using frozen Virchow2 patch features and WSI-bag features with the same 30-instance evidence budget as the companion benchmark. The design separates imbalance severity from the identity of the classes assigned to the head and tail of the training distribution. Discrimination and calibration are evaluated for patch-feature and WSI-bag mitigation methods in their native regimes, including post-hoc temperature scaling fitted on validation predictions.
\end{abstract}

\setcounter{tocdepth}{2}
\tableofcontents
\clearpage

\section{Introduction}
The companion TCGA-UT imbalance benchmark compared class-imbalance mitigation methods in their native input regimes: labeled patch-feature classification and weakly supervised WSI-bag classification. It also showed that class frequency alone is an incomplete explanation for poor classwise performance. Some low-support cancer types remain separable under a frozen pathology foundation model, whereas other classes are difficult even when their support is not the smallest. This report turns that observation into an experimental design question: how should mitigation methods be interpreted when class frequency and inherent class difficulty can be varied deliberately?

TCGA-UT is a useful test bed for this question because it provides both slide labels and patch-level evidence derived from diagnostic whole-slide images in The Cancer Genome Atlas \citep{tomczak2015reviewThe,komura2022universalEncoding}. The native dataset already has a long-tailed slide distribution over 32 cancer types. Native imbalance is realistic, but it entangles prevalence with many other factors: tumor morphology, tissue diversity, slide acquisition, and the representation geometry induced by the frozen encoder. A controlled construction complements the native benchmark by imposing known training-support profiles while holding validation and test data fixed.

The goal is not to introduce a new mitigation method. Instead, the report evaluates representative methods from the companion taxonomy under constructed training distributions. Patch-level methods include cross-entropy, class weighting, balanced sampling, focal loss, and Scholz-style metric losses \citep{scholz2024imbalanceAware}. WSI-bag methods include MIL cross-entropy, class weighting, balanced sampling, focal loss, RankMix, SC-MIL, and MDE-MIL \citep{chen2023rankmix,juyal2024scMil,ling2025mdeMil}. Patch and WSI-bag methods are reported separately because they consume different prediction units and answer regime-specific questions.

\section{Dataset Construction}
\label{sec:dataset-construction}
The construction starts from the same TCGA-UT case-disjoint split protocol as the companion benchmark. For each seed, the validation and test splits are kept fixed, and artificial imbalance is applied only to the training split. The split assignment is patient-disjoint: all slides and feature rows associated with one TCGA participant remain in exactly one of train, validation, or test. This isolates the effect of training support: model selection and held-out evaluation remain comparable across imbalance parameters and mitigation methods without patient leakage.

\subsection{Full-Scale Training Pool}
All 32 TCGA-UT cancer types are retained. Let $C=32$ and let $\mathcal{D}_{\mathrm{train}}$ denote the seed-specific training slide pool. For class $c$, let $a_c$ be the number of available training slides before artificial resampling. The constructed training split must satisfy three constraints:
\begin{equation}
  1 \le n_c \le a_c,\qquad
  \sum_{c=1}^{C} n_c = |\mathcal{D}_{\mathrm{train}}|,\qquad
  c \in \{1,\ldots,C\}.
  \label{eq:full-scale-constraints}
\end{equation}
\noindent The first constraint keeps every class represented and prevents oversampling beyond available slides. The second preserves the full training split size. The third makes the benchmark a 32-class TCGA-UT study throughout.

After slide selection, the input evidence is capped in the same way as in the companion benchmark. Patch classifiers use up to 30 deterministic patches per slide. WSI-bag classifiers use up to 30 deterministic frozen feature instances per slide. In both regimes, Virchow2 features remain frozen \citep{vorontsov2024virchow,zimmermann2024virchow2}. Features are read from the shared cls\_patchmean Virchow2 store ($\mathrm{concat}(\mathrm{CLS}, \mathrm{mean}(\mathrm{patch\ tokens}))$, \num{2560}-dimensional vectors) used by the companion WSI-bag benchmark. The cap prevents slides with more extracted tumor patches from dominating the training objective while keeping the input budget aligned across regimes.

\subsection{Power-Law Training Support}
For an ordered class list $(c_1,\ldots,c_C)$, the intended share for rank $i$ under imbalance parameter $\lambda$ is
\begin{equation}
  p_i(\lambda) =
  \frac{i^{-\lambda}}{\sum_{j=1}^{C} j^{-\lambda}}.
  \label{eq:power-law}
\end{equation}
\noindent When $\lambda=0$, the intended distribution is uniform over ranks. As $\lambda$ increases, early-ranked classes receive more training support and late-ranked classes receive less. Raw targets $|\mathcal{D}_{\mathrm{train}}|p_i(\lambda)$ are converted to integer counts by flooring and assigning residual slides by largest fractional remainder.

The target distribution is then projected onto the feasible set in Equation~\eqref{eq:full-scale-constraints}. Each class receives one guaranteed slide. Remaining slides are allocated according to Equation~\eqref{eq:power-law}; classes whose requested support exceeds their available training support are capped, and the unused mass is redistributed over the uncapped classes using the same power-law ranks. This cap-and-redistribute procedure keeps the constructed split full-scale without dropping any cancer type.

\subsection{Class Order}
\label{sec:class-orders}
The same imbalance severity can have different consequences depending on which classes become frequent. This report therefore uses three class-order regimes. In the \emph{native-prevalence order}, classes are ranked by their TCGA-UT slide support, so the constructed head and tail follow the realistic direction of the native TCGA-UT long tail. In the \emph{easy-to-difficult order}, classes are ranked by decreasing full-support baseline recall from the companion benchmark, averaged across the patch-feature CE and WSI-bag MIL-CE baselines. In the \emph{difficult-to-easy order}, that ranking is reversed. The difficulty orders deliberately couple support with inherent class separability in opposite directions: the first assigns more support to classes that were already easier under balanced/full-support training, whereas the second assigns more support to classes that were difficult despite the frozen Virchow2 representation.

The construction therefore separates support magnitude from the held-out evaluation distribution while also varying the semantic identity of head and tail classes. Frequency is imposed by $\lambda$ and rank; validation and test cases are unchanged. The companion report's class-difficulty analysis remains the interpretive background: if a class performs poorly when it is assigned high training support, scarcity is not the only plausible explanation.

\section{Methods}
\label{sec:methods}
The method taxonomy follows the companion benchmark, but this report uses it to study controlled training distributions rather than only the native TCGA-UT distribution. Cross-entropy is the no-mitigation reference. Data-level methods change minibatch exposure through balanced sampling or feature-space bag augmentation. Algorithm-level methods change the per-example loss through class weighting, focal loss, metric-derived objectives, or prototype-affinity objectives. WSI-specific methods change how slide bags are represented or mixed.

\begin{table}[ht]
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}p{0.12\linewidth}p{0.20\linewidth}p{0.30\linewidth}X@{}}
    \toprule
    Regime & Family & Methods & Controlled question\\
    \midrule
    Patch & Baseline and losses & CE, weighted CE, focal loss & Frequency weighting and hard-example emphasis.\\
    Patch & Metric losses & CE + soft F1, CE + soft MCC & Macro-metric losses with balanced minibatch exposure.\\
    Patch & Specialized & CFAL, divide-and-conquer & Prototype-affinity and expert fusion objectives.\\
    WSI bag & Baseline and losses & MIL CE, weighted CE, focal loss & Slide-level analogues of standard losses.\\
    WSI bag & MIL-specific & Balanced sampling, RankMix, SC-MIL, MDE-MIL & Bag-level sampling, augmentation, contrastive structure, and expert training.\\
    \bottomrule
  \end{tabularx}
  \caption{Method groups evaluated under constructed TCGA-UT training distributions. Patch and WSI-bag methods are compared only within their own input regime.}
  \label{tab:method-groups}
\end{table}

Let $y$ be the class label, $\mathbf{z}\in\mathbb{R}^C$ the logits, and $p_y$ the softmax probability of the true class. Cross-entropy optimizes $-\log p_y$. Weighted cross-entropy multiplies this term by a normalized inverse-frequency weight. Focal loss uses
\begin{equation}
  \mathcal{L}_{\mathrm{focal}}(\mathbf{z},y)
  = -(1-p_y)^\gamma \log p_y,
  \label{eq:focal}
\end{equation}
\noindent so confidently classified examples contribute less to the gradient. The soft-F1 and soft-MCC variants add differentiable approximations of macro evaluation metrics to CE, following the imbalance-aware medical-image objectives of \citet{scholz2024imbalanceAware}. RankMix, SC-MIL, and MDE-MIL are retained as WSI-bag methods because their mechanisms operate on bags rather than isolated patch embeddings \citep{chen2023rankmix,juyal2024scMil,ling2025mdeMil}.

\section{Evaluation Protocol}
\label{sec:evaluation}
Each constructed split is indexed by seed, class-order regime, and imbalance parameter $\lambda$. The main reporting points are $\lambda=0.0$, $\lambda=1.0$, and $\lambda=1.3$. For every method and every constructed regime (class order $\times$ $\lambda$), hyperparameters are selected by validation macro F1 across three seeds; ties are broken by validation balanced accuracy. Baseline CE and MIL CE use fixed protocol settings. Selected configurations are evaluated on the fixed test split. Macro F1 is the primary discrimination metric because it weights cancer types equally. Balanced accuracy and accuracy are reported as complementary discrimination metrics. Calibration is summarized with negative log-likelihood, multiclass Brier score, and expected calibration error. For each trained model, a scalar temperature is fitted on the validation probabilities and applied to the corresponding test probabilities, so raw and temperature-scaled calibration can be compared without changing discrimination metrics.

The comparison is intentionally regime-specific. Patch-feature methods are compared only with other patch-feature methods, and WSI-bag methods are compared only with other WSI-bag methods. The two regimes share the case-disjoint splits, frozen Virchow2 representation family, and 30-instance evidence budget, but they differ in prediction unit and aggregation mechanism.

\section{Results}
\label{sec:results}
The result analysis asks how methods behave across the three class-order regimes and across imbalance severities $\lambda \in \{0.0, 1.0, 1.3\}$. Table~\ref{tab:constructed-split-summary} and Figure~\ref{fig:constructed-support} show the imposed support profiles. Tables~\ref{tab:patch-results} and~\ref{tab:wsi-results} compare mitigation methods within the patch-feature and WSI-bag regimes across all nine constructed regimes.

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/constructed_split_summary.tex}{%
    \input{outputs/tables/constructed_split_summary.tex}%
  }{%
    \begin{tabular}{llll}
      \toprule
      Order & $\lambda$ & Training slides & Minimum class support\\
      \midrule
      \multicolumn{4}{c}{Generated split summary unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{Constructed training-support summaries by class-order regime and imbalance severity.}
  \label{tab:constructed-split-summary}
\end{table}

\begin{figure}[ht]
  \centering
  \IfFileExists{outputs/figures/constructed_support_by_rank.png}{%
    \includegraphics[width=0.86\linewidth]{constructed_support_by_rank.png}%
  }{%
    \fbox{\parbox{0.80\linewidth}{\centering Constructed support figure unavailable.}}%
  }
  \caption{Constructed training support by class rank for the main imbalance settings.}
  \label{fig:constructed-support}
\end{figure}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/result_summary_patch.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/result_summary_patch.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{Patch result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{Patch-feature test results for validation-selected configurations under constructed training distributions.}
  \label{tab:patch-results}
\end{table}

At the patch-feature level, the dominant pattern across all nine constructed regimes is stability: neither changing the class order nor increasing $\lambda$ produces consistent shifts in balanced accuracy or macro F1 (Table~\ref{tab:patch-results}). CFAL achieves the highest balanced accuracy of all patch methods (\num{0.763}--\num{0.766}) and macro F1 (\num{0.766}--\num{0.768}), with virtually no change across class orders or imbalance severities. The CE-plus-soft-F1 variant follows with balanced accuracy \num{0.743}--\num{0.759}, the widest within-method range of any patch method but still spanning less than two percentage points. Weighted CE, focal loss, and balanced sampling cluster tightly in the \num{0.740}--\num{0.744} balanced-accuracy band. CE-plus-soft-MCC occupies a similar range (\num{0.733}--\num{0.751}), with somewhat higher variance across seeds at the strongest imbalance. Divide-and-conquer is a consistent outlier at \num{0.714} balanced accuracy regardless of class order or $\lambda$. The absence of systematic rank reversals between methods across the three class-order regimes is the salient patch-level result: altering which classes are placed in the training head, and how aggressively, does not materially change discrimination when frozen Virchow2 patch features are used.

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/result_summary_wsi_bag.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/result_summary_wsi_bag.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{WSI-bag result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{WSI-bag test results for validation-selected configurations under constructed training distributions.}
  \label{tab:wsi-results}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/result_calibration_patch.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/result_calibration_patch.tex}}%
  }{%
    \begin{tabular}{lccccccc}
      \toprule
      Method & NLL & NLL+TS & Brier & Brier+TS & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{8}{c}{Patch calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{Patch-feature calibration before and after validation-fitted temperature scaling.}
  \label{tab:patch-calibration}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/result_calibration_wsi_bag.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/result_calibration_wsi_bag.tex}}%
  }{%
    \begin{tabular}{lccccccc}
      \toprule
      Method & NLL & NLL+TS & Brier & Brier+TS & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{8}{c}{WSI-bag calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{WSI-bag calibration before and after validation-fitted temperature scaling.}
  \label{tab:wsi-calibration}
\end{table}

The WSI-bag regime reveals a clearer separation between method families than the patch regime, while also showing stability across class orders (Table~\ref{tab:wsi-results}). MDE-MIL achieves the highest balanced accuracy (\num{0.849}--\num{0.866}) and macro F1 (\num{0.859}--\num{0.870}) across all nine constructed regimes. RankMix is the closest competitor, with balanced accuracy \num{0.850}--\num{0.868} and macro F1 \num{0.845}--\num{0.852}. SC-MIL follows at \num{0.845}--\num{0.854} balanced accuracy, and balanced-sampling MIL reaches \num{0.840}--\num{0.857}, with its largest value under native-prevalence ordering at $\lambda=1.3$. Weighted MIL and focal MIL occupy the lower tier at roughly \num{0.822}--\num{0.852} balanced accuracy, with weighted MIL showing noticeably more variance across seeds at $\lambda=1.3$. Within each WSI-bag method, performance shifts by at most two percentage points across the three class-order regimes at any fixed $\lambda$. The ranking of method families—MDE-MIL and RankMix above SC-MIL above loss-based variants—is consistent across all nine constructed regimes, confirming that the advantage of WSI-specific bag-composition mechanisms is not contingent on which classes occupy the training head.

\section{Discussion}
\label{sec:discussion}
The central finding of the controlled construction is that frozen Virchow2 patch representations absorb most of the variation introduced by different class orderings and imbalance severities. Across all nine constructed regimes, both patch-feature and WSI-bag methods shift by less than two percentage points in balanced accuracy and macro F1. This stability holds even under the difficult-to-easy ordering, which deliberately assigns more training support to the cancer types whose frozen features overlap most with neighbouring classes—exactly the configuration the companion benchmark identified as a risk factor for poor classwise recall. Evidently, the absolute support provided under the full-scale construction is already sufficient for the frozen representation to maintain near-equivalent discrimination regardless of how training mass is distributed across classes.

Patch-feature results cluster tightly across all conditions. CFAL achieves the highest balanced accuracy without dependence on class order or $\lambda$; divide-and-conquer is the weakest patch method throughout. Between these extremes, CE-plus-soft-F1, CE-plus-soft-MCC, weighted CE, focal loss, and balanced sampling occupy a narrow band roughly five percentage points wide. The absence of a strong imbalance effect on patch methods is consistent with the companion report's observation that a high-quality frozen encoder compresses many patch-level method differences: when features are discriminative, class-frequency adjustments in the loss or sampling schedule have little residual work to do.

WSI-bag methods show more differentiation between families, but their ranking across class orders is stable. MDE-MIL and RankMix maintain their lead over loss-based MIL variants under all three orderings, including the difficult-to-easy regime where one might expect aggregation-based methods to benefit from the larger support given to structurally ambiguous classes. The persistence of this ranking argues that the advantage of WSI-specific bag-composition mechanisms reflects the bag-level aggregation problem in general, not a specific alignment between method design and distribution shape.

Together, these results extend the companion benchmark's conclusion in a controlled direction. The companion showed that class frequency is not the whole story for classwise performance in the native TCGA-UT distribution. The present experiment shows that when frequency is imposed directly, the constructed support profile has minimal effect on aggregate discrimination for either prediction unit. The prediction-unit dimension—patch versus WSI-bag—continues to dominate method comparisons: the gap between regimes exceeds the within-regime variation due to class order or $\lambda$ by a large margin.

\section{Conclusion}
This report defines a full-scale controlled TCGA-UT imbalance benchmark that keeps all 32 cancer types, preserves patient-disjoint validation and test splits, and varies the training support profile under three class-order regimes---native prevalence, easy-to-difficult, and difficult-to-easy---each at imbalance severities $\lambda \in \{0.0, 1.0, 1.3\}$. The primary finding across all nine constructed regimes is robustness: discrimination metrics for both patch-feature and WSI-bag methods shift by less than two percentage points as class order and $\lambda$ vary, indicating that frozen Virchow2 patch representations contain sufficient class-discriminative structure to maintain near-equivalent performance regardless of the constructed training support profile. At the patch level, CFAL achieves the highest balanced accuracy with no regime dependence; divide-and-conquer is the weakest patch method throughout. At the WSI-bag level, MDE-MIL and RankMix consistently outperform loss-based MIL variants under all three class-order regimes, an advantage that reflects bag-level aggregation mechanisms rather than alignment with a particular distribution shape. The regime-invariant ranking of WSI-bag method families, combined with the overall patch-level insensitivity to class order, suggests that the prediction unit is a stronger determinant of method utility than the imposed training distribution under this full-scale, frozen-feature setting.

\clearpage
\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
