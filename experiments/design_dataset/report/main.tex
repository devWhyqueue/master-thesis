\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage[authoryear,round]{natbib}
\usepackage{siunitx}
\sisetup{group-separator={,}, group-minimum-digits=4}

\graphicspath{{outputs/figures/}{../../experiments/class_imbalance/outputs/figures/}}

\title{Extending Class-Imbalance Mitigation Benchmarks to External Datasets:\\A Native BRACS Benchmark}
\author{Yannik Queisler}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
\noindent A prior study benchmarked class-imbalance mitigation methods on TCGA-UT, a pan-cancer whole-slide-image cohort with a native, moderately imbalanced slide distribution \citep{queisler2026imbalanceTCGA}. This report extends the same benchmark machinery to additional datasets, keeping the frozen Virchow2 representation, the bounded evidence budget, and the patient-disjoint evaluation protocol fixed, and asks whether the method comparison remains meaningful when the data regime changes. The first external dataset is BRACS, an organ-specific H\&E breast-lesion cohort evaluated on its native seven-subtype distribution; further datasets are planned. Both patch-feature and whole-slide-image (WSI) feature-bag method families are compared, each within its own prediction regime, and a plain cross-entropy no-mitigation baseline (CE, MIL CE) is added so mitigation gains are measured against an unweighted reference. On BRACS's native seven-subtype distribution (head-to-tail ${\approx}2.3{:}1$), CFAL leads the patch regime by a clear margin over both the unweighted CE baseline and the other mitigation methods, while ProGAN synthetic augmentation is the weakest patch method overall, falling slightly below unweighted CE. In the WSI-bag regime the unweighted MIL CE baseline is competitive with, and in several cases stronger than, dedicated mitigation methods---only focal loss and SC-MIL clearly exceed it. Both regimes remain substantially miscalibrated after temperature scaling relative to what is achievable on the pan-cancer TCGA-UT cohort, reflecting the smaller training pool and the difficulty of fine-grained breast-lesion subtyping.
\end{abstract}

\setcounter{tocdepth}{2}
\tableofcontents
\clearpage

\section{Introduction}
A companion study compared class-imbalance mitigation methods under the native, moderately imbalanced slide distribution of TCGA-UT (head-to-tail ${\approx}28{:}1$) across 32 cancer types \citep{queisler2026imbalanceTCGA,tomczak2015reviewThe,komura2022universalEncoding}. That benchmark fixes a frozen Virchow2 encoder, a bounded per-slide evidence budget, and patient-disjoint splits, so that the only thing being compared is how each mitigation method reshapes minibatch exposure or the per-example loss. The natural next question is external validity: does the same benchmark machinery stay meaningful when it is pointed at a different dataset, with a different organ, a different label set, and a different native class distribution?

This report takes the first step by extending the benchmark to \emph{native} external datasets rather than to a controlled reconstruction of TCGA-UT. The design is deliberately conservative: the representation family (frozen Virchow2), the 30-instance evidence budget, the patient-disjoint split protocol, the method families, and the validation-based model-selection rule are all carried over unchanged, and only the dataset is swapped. A dataset therefore acts as an external check on the benchmark rather than as a new experimental variable.

The first external dataset is BRACS, an H\&E breast-pathology dataset with \num{547} WSIs and \num{4539} regions of interest (ROIs) from \num{189} patients, annotated by consensus of three board-certified pathologists into three lesion types and seven subtypes \citep{brancati2022bracs}. The atypical categories are clinically important and comparatively scarce, so BRACS is a genuine class-imbalance setting without any resampling. Unlike TCGA-UT it is organ-specific, has far fewer labels, and provides lesion-focused ROI annotations; these differences are exactly what make it a useful external check. Additional datasets are planned and will slot into the same benchmark.

Both regimes from the companion study are carried over. Patch-feature methods include cross-entropy, class weighting, balanced sampling, focal loss, Scholz-style metric losses, CFAL, OKO set learning, and ProGAN synthetic augmentation \citep{scholz2024imbalanceAware}. WSI-bag methods include cross-entropy, class weighting, balanced sampling, focal loss, RankMix, SC-MIL, and MDE-MIL \citep{chen2023rankmix,juyal2024scMil,ling2025mdeMil}. Patch and WSI-bag methods are reported separately because they consume different prediction units and answer regime-specific questions.

\section{Datasets}
\label{sec:datasets}
The benchmark evaluates each dataset on its \emph{native} class distribution: the observed per-class support is measured after a patient-disjoint preparation and used directly, without any resampling or power-law reconstruction. This report covers BRACS; further datasets will be added under the same protocol.

\subsection{BRACS}
BRACS is an organ-specific H\&E breast-lesion dataset rather than a pan-cancer dataset, and its annotations are lesion-subtype labels rather than cancer-type labels. It contains \num{547} WSIs and \num{4539} ROIs from \num{189} patients, annotated by consensus of three board-certified pathologists \citep{brancati2022bracs}. The labels form three lesion types---benign, atypical, and malignant---and seven subtypes: normal tissue (N), pathological benign (PB), usual ductal hyperplasia (UDH), flat epithelial atypia (FEA), atypical ductal hyperplasia (ADH), ductal carcinoma in situ (DCIS), and invasive carcinoma (IC).

The benchmark uses the seven-subtype target because it is the level at which class imbalance is most relevant. The atypical categories FEA and ADH are clinically important precancerous lesions and are less common than many benign or malignant samples, so collapsing BRACS to the three lesion types would hide part of the rare-class problem. Patient identifiers from the BRACS summary sheet define the split unit, and the prepared benchmark enforces patient-disjoint train, validation, and test partitions across three seeds---matching the leakage-control standard used for TCGA-UT.

BRACS evidence is derived from the ROI set. Each annotated ROI is tiled deterministically into $256\times256$ RGB patches, capped at 30 tiles per ROI to match the evidence budget used for the companion study. Patch methods consume one frozen Virchow2 feature per ROI tile \citep{vorontsov2024virchow,zimmermann2024virchow2}, represented as the concatenation of the CLS token and the mean of the patch tokens ($\num{2560}$-dimensional vectors). The WSI-bag pipeline consumes grouped ROI-tile features as bounded bags, preserving the same patch-versus-bag comparison while avoiding an additional tissue-detection step on full WSIs. This choice prioritizes label fidelity: all training instances originate from pathologist-selected lesion regions rather than from unlabeled background tissue.

After patch extraction, \num{387} of the \num{547} published WSIs contribute at least one annotated ROI tile; the remaining slides have no tiled ROI annotations available. The prepared benchmark covers \num{151} patients, the full \num{4539} ROI metadata records, and \num{98282} extracted patches (Table~\ref{tab:bracs-dataset-summary}). The seven-subtype WSI support spans PB (\num{196} slides) at the head to DCIS (\num{87} slides) at the tail, a native head-to-tail ratio of approximately 2.25:1 (Figure~\ref{fig:bracs-class-distribution}). This is mild imbalance relative to the pan-cancer TCGA-UT cohort, but it reflects the true clinical distribution of breast-lesion subtypes and is used as-is.

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_dataset_summary.tex}{%
    \input{outputs/tables/bracs_dataset_summary.tex}%
  }{%
    \begin{tabular}{ll}
      \toprule
      Property & Value\\
      \midrule
      Patients & \num{151}\\
      Whole-slide images & \num{387}\\
      Regions of interest & \num{4539}\\
      Subtype labels & N, PB, UDH, FEA, ADH, DCIS, IC\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS dataset properties used by the native benchmark, after patient-disjoint preparation and ROI tiling.}
  \label{tab:bracs-dataset-summary}
\end{table}

\begin{figure}[ht]
  \centering
  \IfFileExists{outputs/figures/bracs_class_distribution.png}{%
    \includegraphics[width=0.72\linewidth]{bracs_class_distribution.png}%
  }{%
    \fbox{\parbox{0.70\linewidth}{\centering BRACS native subtype distribution figure unavailable.}}%
  }
  \caption{Native BRACS WSI support by seven-subtype label after patient-disjoint preparation. The distribution is used directly as the benchmark's native class-imbalance setting.}
  \label{fig:bracs-class-distribution}
\end{figure}

\section{Methods}
\label{sec:methods}
The method taxonomy follows the companion benchmark \citep{queisler2026imbalanceTCGA}. Cross-entropy is the no-mitigation reference. Data-level methods change minibatch exposure through balanced sampling or synthetic augmentation. Algorithm-level methods change the per-example loss through class weighting, focal loss, metric-derived objectives, prototype-affinity objectives, or OKO set supervision. WSI-specific methods change how slide bags are represented or mixed.

\begin{table}[ht]
  \centering
  \footnotesize
  \begin{tabularx}{\linewidth}{@{}p{0.22\linewidth}p{0.24\linewidth}X@{}}
    \toprule
    Family & Methods & Regime\\
    \midrule
    No-mitigation baseline & CE, MIL CE & Patch, WSI bag\\
    Data-level sampling and augmentation & Balanced sampling, RankMix & Patch, WSI bag\\
    Algorithm-level losses & Weighted CE, Focal loss, CFAL & Patch (CFAL); Patch and WSI bag (others)\\
    Hybrid sampling--loss & CE + soft F1, CE + soft MCC, OKO, MDE-MIL & Patch (first three, OKO); WSI bag (MDE-MIL)\\
    Synthetic data generation & ProGAN augmentation & Patch\\
    Representation and architecture & SC-MIL & WSI bag\\
    \bottomrule
  \end{tabularx}
  \caption{Method families evaluated on the native BRACS benchmark, following the taxonomy of \citet{queisler2026imbalanceTCGA}. CE and MIL CE are unweighted cross-entropy baselines, added so mitigation gains are measured against a genuine no-mitigation reference. Patch and WSI-bag methods are compared only within their own input regime.}
  \label{tab:method-groups}
\end{table}

Full method definitions follow \citet{queisler2026imbalanceTCGA} and \citet{scholz2024imbalanceAware}. RankMix, SC-MIL, and MDE-MIL are WSI-bag methods whose mechanisms operate on bags rather than isolated patch embeddings \citep{chen2023rankmix,juyal2024scMil,ling2025mdeMil}. The plain cross-entropy baselines (CE for the patch regime, MIL CE for the WSI-bag regime) apply uniform class weights and no resampling, so any improvement from a mitigation method is measured relative to doing nothing.

\section{Evaluation Protocol}
\label{sec:evaluation}
The benchmark evaluates each dataset on its native class distribution. Hyperparameters are selected independently for every method: for each candidate configuration the validation macro F1 is averaged over the three patient-disjoint seeds, and the configuration with the highest mean is chosen, with ties broken by mean validation balanced accuracy. A single configuration is therefore fixed per method (shared across seeds). Selected configurations are evaluated on the fixed test split, and results are reported as mean $\pm$ standard deviation across seeds. Macro F1 is the primary discrimination metric because it weights classes equally; balanced accuracy and accuracy are reported as complementary discrimination metrics.

The comparison is intentionally regime-specific. Patch-feature methods are compared only with other patch-feature methods, and WSI-bag methods are compared only with other WSI-bag methods. The two regimes share the patient-disjoint splits, the frozen Virchow2 representation family, and the 30-instance evidence budget, but they differ in prediction unit and aggregation mechanism.

Calibration is evaluated alongside discrimination using expected calibration error \citep[ECE;][]{naeini2015obtaining,guo2017calibration}. For each method and seed, a single temperature is fit by minimizing negative log likelihood on the validation split and applied unchanged to the test score matrix before softmax \citep{guo2017calibration}. Tables~\ref{tab:bracs-calibration-patch} and~\ref{tab:bracs-calibration-wsi} report ECE before and after temperature scaling with the fitted temperature.

\section{Results}
\label{sec:results}
The patch-feature and WSI-bag regimes are reported separately (Tables~\ref{tab:bracs-patch-results} and~\ref{tab:bracs-wsi-results}). Scores are specific to the seven-class breast-lesion subtyping task and are not directly comparable to the 32-class TCGA-UT benchmark: the label set, task difficulty, and cohort size all differ.

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_result_summary_patch.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/bracs_result_summary_patch.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{BRACS patch result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native patch-feature test results (accuracy, balanced accuracy, and macro F1) for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:bracs-patch-results}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_result_summary_wsi_bag.tex}{%
    \resizebox{\linewidth}{!}{\input{outputs/tables/bracs_result_summary_wsi_bag.tex}}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & Accuracy & Balanced accuracy & Macro F1\\
      \midrule
      \multicolumn{4}{c}{BRACS WSI-bag result table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native WSI-bag test results for validation-selected configurations, mean $\pm$ standard deviation across patient-disjoint seeds.}
  \label{tab:bracs-wsi-results}
\end{table}

In the patch regime, CFAL leads on all three metrics (BAcc \num{0.512}, F1 \num{0.499}), \num{2.2} points of balanced accuracy ahead of the next-best method (CE + soft F1, BAcc \num{0.490}) and \num{5.5} points ahead of the unweighted CE baseline (BAcc \num{0.457}, F1 \num{0.443}). The remaining methods---Weighted CE, focal loss, CE + soft MCC, and balanced sampling---cluster tightly between BAcc \num{0.458} and \num{0.468}, indistinguishable from the CE baseline within one standard deviation (all $\sigma \approx 0.03$--$0.08$). OKO (BAcc \num{0.482}) sits between this cluster and CFAL. ProGAN augmentation is the weakest method in the regime (BAcc \num{0.455}, F1 \num{0.437}), fractionally below the unweighted CE baseline on every metric---unlike on the companion TCGA-UT benchmark, where ProGAN led at the mildest severity, BRACS's native imbalance never becomes severe enough for synthetic augmentation to pay off. The overall picture is that most patch mitigation methods provide little measurable lift over unweighted cross-entropy on BRACS's mild native imbalance, and one (ProGAN) falls slightly below it; only CFAL and, to a lesser extent, CE + soft F1 and OKO separate from the no-mitigation baseline.

In the WSI-bag regime, the unweighted MIL CE baseline (BAcc \num{0.446}, F1 \num{0.430}) is competitive with most dedicated mitigation methods: it outperforms Weighted CE (BAcc \num{0.415}), balanced sampling (BAcc \num{0.400}), RankMix (BAcc \num{0.408}), and MDE-MIL (BAcc \num{0.426}) on balanced accuracy. Only focal loss (BAcc \num{0.469}, the regime leader) and SC-MIL (BAcc \num{0.453}, F1 \num{0.472}, the regime's highest macro F1) clearly exceed the CE baseline. Between-seed standard deviations are large throughout this regime (up to $\sigma \approx 0.10$--$0.13$ for RankMix and SC-MIL), so individual rankings should be read as numerical leads rather than statistically separated outcomes; the qualitative pattern---that several bag-composition and resampling methods do not clearly beat plain MIL CE on BRACS---is nonetheless the direct motivation for including the baseline. Balanced sampling is the weakest method in this regime (BAcc \num{0.400}, F1 \num{0.395}), below the unweighted baseline on every metric.

\paragraph{Probability Calibration.}
\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_calibration_patch.tex}{%
    \input{outputs/tables/bracs_calibration_patch.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{BRACS patch calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native patch-feature calibration: ECE before (raw) and after (TS) temperature scaling, with the fitted temperature $T$, mean $\pm$ standard deviation across seeds.}
  \label{tab:bracs-calibration-patch}
\end{table}

\begin{table}[ht]
  \centering
  \IfFileExists{outputs/tables/bracs_calibration_wsi_bag.tex}{%
    \input{outputs/tables/bracs_calibration_wsi_bag.tex}%
  }{%
    \begin{tabular}{lccc}
      \toprule
      Method & ECE & ECE+TS & $T$\\
      \midrule
      \multicolumn{4}{c}{BRACS WSI-bag calibration table unavailable.}\\
      \bottomrule
    \end{tabular}%
  }
  \caption{BRACS native WSI-bag calibration: ECE before and after temperature scaling, with the fitted temperature $T$. Columns as in Table~\ref{tab:bracs-calibration-patch}.}
  \label{tab:bracs-calibration-wsi}
\end{table}

In the patch regime, raw (pre-scaling) miscalibration is severe for most methods: ECE ranges from \num{0.28} to \num{0.42}, requiring large temperature corrections ($T \approx 3$--\num{9}) to reach post-scaling ECE of \num{0.04}--\num{0.08}. OKO is the exception, with by far the lowest raw ECE (\num{0.146}) and the smallest fitted temperature ($T\approx\num{1.47}$), consistent with its behaviour on the companion TCGA-UT benchmark. CFAL again shows the CFAL-specific pattern of a near-degenerate low temperature ($T\approx\num{0.058}$) alongside the highest post-scaling ECE (\num{0.068}) of any patch method, echoing its affinity-based outputs being nearly flat prior to scaling. Focal loss achieves the lowest post-scaling ECE (\num{0.038}). ProGAN augmentation calibrates similarly to the unweighted baselines (raw ECE \num{0.394}, $T\approx\num{6.34}$, post-scaling ECE \num{0.053}), showing no distinctive miscalibration pattern despite its weaker discrimination. In the WSI-bag regime, calibration is uniformly worse than the patch regime and than the companion TCGA-UT benchmark: post-scaling ECE ranges from \num{0.112} (SC-MIL, the best-calibrated method) to \num{0.155} (MIL CE and Weighted CE, tied for worst). RankMix has the lowest raw ECE (\num{0.152}) and smallest temperature ($T\approx\num{1.16}$) of any WSI-bag method, but its post-scaling ECE (\num{0.133}) is not the best. The elevated residual miscalibration across all BRACS methods, relative to TCGA-UT, reflects the smaller training pool and the intrinsic difficulty of seven-class fine-grained subtyping rather than a failure specific to any one method.

\section{Discussion}
\label{sec:discussion}
The benchmark's central design change from the companion TCGA-UT study is the addition of an unweighted no-mitigation baseline (CE, MIL CE), and the two regimes answer the question it was added to ask quite differently. In the patch regime, most mitigation methods do not clearly separate from the CE baseline on BRACS's mild native imbalance (${\approx}2.3{:}1$)---Weighted CE, focal loss, CE + soft MCC, and balanced sampling all sit within noise of unweighted cross-entropy, and ProGAN augmentation falls fractionally below it. Only CFAL, and to a lesser extent CE + soft F1 and OKO, produce a measurable gain. This is a plausible consequence of the imbalance itself being mild: with a native ratio an order of magnitude below TCGA-UT's ${\approx}28{:}1$, there may simply be less headroom for reweighting or resampling to help. ProGAN's reversal is particularly informative: on TCGA-UT it led at the mildest constructed severity before collapsing as tail-class support fell to single-digit patch counts at the harshest severity. BRACS's native imbalance never reaches that regime, so ProGAN never gets the tail-scarcity conditions under which synthetic augmentation would be expected to help, and it is left carrying only the risk of the generative step (distributional mismatch between synthetic and real patches) without the corresponding benefit.

The WSI-bag regime tells a sharper version of the same story: several mitigation methods (Weighted CE, balanced sampling, RankMix, MDE-MIL) score \emph{below} the unweighted MIL CE baseline on balanced accuracy. Only focal loss and SC-MIL clearly improve on it. This is exactly the failure mode a no-mitigation baseline is meant to surface, and it would have been invisible in a benchmark that only compared mitigation methods against each other---the companion TCGA-UT report, which lacked this baseline, could not have detected it. Whether this reflects BRACS's smaller training pool (108 native WSIs after preparation, versus TCGA-UT's constructed pools of over 1000 slides), the milder native imbalance giving resampling less to correct, or a genuine difference in how bag-composition methods behave outside a pan-cancer setting cannot be distinguished from a single external dataset; it is a hypothesis for the next dataset in this benchmark to test.

Calibration adds a second, independent axis on which BRACS is harder than TCGA-UT: even the best-calibrated method in each regime (focal loss for patch, SC-MIL for WSI-bag) needs temperature scaling to reach an ECE that is still \num{2}--\num{3}$\times$ worse than typical TCGA-UT post-scaling values. This is consistent with the smaller training pool and finer-grained label set rather than a method-specific weakness, since the pattern holds across every method in both regimes.

\section{Conclusion}
\label{sec:conclusion}
This report extended the class-imbalance mitigation benchmark introduced for TCGA-UT \citep{queisler2026imbalanceTCGA} to a first external dataset, BRACS, keeping the frozen Virchow2 representation, the 30-instance evidence budget, and the patient-disjoint evaluation protocol unchanged and adding an unweighted cross-entropy baseline (CE, MIL CE) so mitigation gains are measured against doing nothing. Evaluated on BRACS's native seven-subtype distribution (${\approx}2.3{:}1$ head-to-tail), the patch regime shows CFAL leading by a clear margin (BAcc \num{0.512} vs.\ \num{0.457} for CE), with most other mitigation methods statistically indistinguishable from the no-mitigation baseline and ProGAN augmentation---the leader at mild severity on the companion TCGA-UT benchmark---falling fractionally below it (BAcc \num{0.455}). The WSI-bag regime shows that several mitigation methods---Weighted CE, balanced sampling, RankMix, and MDE-MIL---do not clearly improve on the unweighted MIL CE baseline; only focal loss and SC-MIL do. Both regimes remain substantially more miscalibrated after temperature scaling than the companion TCGA-UT benchmark, consistent with BRACS's smaller training pool and harder seven-class fine-grained subtyping task.

These results validate the benchmark's external applicability while also showing that method rankings are not universal: CFAL's patch-level lead, ProGAN's reversal from TCGA-UT leader to BRACS laggard, and several WSI-bag methods' failure to beat plain cross-entropy are all new findings relative to TCGA-UT, and none would have been visible without a native external dataset and a genuine no-mitigation reference. Further external datasets remain to be added under the same protocol; the controlled power-law severity construction previously explored for TCGA-UT and BRACS has been moved to a separate follow-up experiment (see \texttt{controlled\_imbalance\_experiment.md}) rather than folded into this native-benchmark report.

\clearpage
\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
