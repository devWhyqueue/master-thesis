# 2026-06-11 Meeting

## Augustin’s Design Dataset

* Continuation of the first experiment:

  * After analyzing mitigation methods on native TCGA-UT, the next step is to evaluate them in controlled imbalance settings.
* Current work:

  * Adapting the code to align with my imbalance experiment
  * Transforming the bullet-point report into a more paper-style write-up
  * Adding further analyses, including calibration
  * Hydra jobs for the three different settings are already running
* Next step:

  * Generalize the setup to additional popular CPath datasets, likely 2–3 datasets.

## Next Experiments

1. **Class difficulty as a predictor of class performance**

   * Hypothesis: Class difficulty is an important predictor of class-level performance.
   * Research questions:

     * Which methods exist to measure class difficulty?
     * Possible direction: k-NN on embeddings in a balanced setting.
     * Which methods can improve performance for difficult classes?
   * Next step: Literature review.

2. **Combining findings into a new method proposal**

   * After analyzing different mitigation method categories across native datasets and controlled imbalance settings, combine the findings into a new method proposal.

## Input / Feedback

* Patch-level applications are rare.
* In MIL settings, the number of patches is usually much larger than 30.
* Include OKO set learning as a method.
* Add a low-data setting experiment.
* Ensure that the total number of samples across all classes remains constant when modifying imbalance.

  * Otherwise, the experiment risks comparing low-data and high-data domains rather than isolating imbalance effects.
* Add UMAP visualizations of the features.
